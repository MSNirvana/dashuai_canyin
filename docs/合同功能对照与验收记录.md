# 大帅餐饮软件开发服务合同：功能对照与验收记录

> 本记录对应合同文件：`/Users/gaoyunhong/Desktop/大帅餐饮/软件开发服务合同_.pdf`
> 本轮原则：抖音小程序按用户要求暂缓；不把未配置的第三方能力伪装为已完成。

## 1. 本轮验收环境

- 验收时间：2026-09-11（本地时区 GMT+8）
- 服务端：Express + Prisma，`http://localhost:3000`
- 数据库：本地 MySQL，数据库 `dashuai`
- 缓存：本地 Redis
- 存储：腾讯云 COS（`dashuai-1485028436` / `ap-beijing`），2026-09-14 已完成真实读写与签名播放复验
- AI：后台真实场景链路可用，失败兜底仍保留
- 渲染：FFmpeg Worker 异步队列模式
- 健康检查：`GET /healthz` 返回 HTTP 200

## 2. 合同功能对照

| 合同范围 | 当前状态 | 说明 |
|---|---|---|
| 微信小程序端 | 已实现 / 本地已验证 | 登录、门店、菜品、人设、创作、文案、分镜、素材、预览、合成、历史成片、会员与积分均有页面和接口 |
| 抖音小程序端 | 暂缓 | 按用户本轮明确要求暂不处理，不纳入本轮阻塞 |
| 商家基础信息 | 已实现 / 本地已验证 | 门店、菜品、门店级人设、素材归属和多租户隔离已覆盖 |
| AI 餐饮文案 | 已实现 / 本地已验证 | 流量款、介绍款、质量款；场景配置、真实模型调用、兜底和积分账务均已接入 |
| AI 分镜脚本 | 已实现 / 本地已验证 | 简单/复杂/精细三档；镜头数量、景别、时长、台词、画面要求和镜头库匹配已覆盖 |
| 按镜头上传素材 | 已实现 / COS 已真实验证 | COS 临时凭证、真实对象上传、素材落库、私有签名播放和下载已验证；大视频分片/弱网续传仍需专项验收 |
| 基础粗剪 | 本地已实现 / 外部环境待验收 | BASIC/AI 队列、FFmpeg Worker、基础裁剪与硬切拼接链路已实现；需使用真实视频素材复核成片质量 |
| 后台会员套餐 | 已实现 / 本地已验证 | 套餐查询、后台配置、会员权益发放和续期逻辑已实现 |
| 微信支付 | 代码已实现 / 外部环境待验收 | 回调解密、appid/mchid/金额校验、CAS 幂等已实现；需真实商户号、证书和真机支付 |
| 会员到期续费提醒 | 已实现 / 本地已验证 | 新增 7/3/1 天站内提醒、幂等扫描、服务启动 sweeper、用户查询和已读接口；微信订阅消息真实发送待配置 |
| 短信验证码 | 开发模式可用 / 真实服务待接入 | 验证码哈希、过期、重试、冷却和限额已实现；生产未配置真实 provider 时 fail-closed，不能伪造发送成功 |
| 镜头库及拍摄技巧 | 已实现 / 本地已验证 | 18 条、9 类，包含基础手法和 AI 匹配；示范视频字段和播放入口已有 |
| 镜头库示范视频内容 | 甲方素材待提供 | 当前 seed 无真实示范视频，不凭空伪造；后台可配置 `demoVideoKey/demoCoverKey` |
| 配乐风格建议 | 已注册场景 / 产品闭环待补 | `bgm_select` 场景已注册，但音乐版权采购不在合同范围；后续可提供无版权风格建议 |
| 字幕文案 | 基础能力待补 | `title_overlay` 已注册，复杂字幕动画不属于合同范围；真实 TTS/字体/字幕效果需外部环境验收 |
| 基础拍摄/剪辑教程 | 部分具备 | 镜头库 tips 和创作提示已具备；独立教程内容和示范视频待运营素材补齐 |
| 源码、配置和文档 | 项目内已具备 / 最终交付包待整理 | 代码、schema、接口和部署相关文档已在仓库；需最终打包并由双方确认版本 |
| 微信上线协助 | 外部流程待执行 | 需要小程序主体、合法域名、类目、审核和开发者工具/真机环境 |

## 3. 本轮真实全链路结果

脚本：`server/scripts/e2e-smoke.mjs`

脚本现在默认保留数据，只有显式设置 `CLEAN_TEST_DATA=true` 才会软删除测试门店。

已通过链路：

```text
开发登录
→ 创建门店
→ 创建菜品
→ 保存门店级人设
→ 第二门店隔离校验
→ 创建创作
→ AI 文案
→ AI 分镜
→ 本地上传确认
→ 绑定 6 个分镜素材
→ 拼图预览
→ 提交异步合成
→ 查询任务列表与详情
→ 查询积分流水
→ 查询 AI 日志
→ 查询当前会员
→ 查询镜头库、分类过滤与基础手法
```

结果：全部通过。

## 4. 保留的验收数据

- 商家 ID：`1`
- 验收门店 ID：`35`
- 验收创作 ID：`51`
- 验收渲染任务 ID：`28`
- 验收素材 ID：`47`
- 分镜：6 个，均已绑定素材
- 账务流水：查询到 336 条历史流水，包含 AI 与渲染冻结/消费/释放记录
- AI 调用日志：查询到 102 条
- 会员：有效，结束时间为 `2026-10-08T07:03:38.617Z`

详细控制台记录：`docs/真实全链路测试记录-2026-09-11.log`

COS 切换后回归记录：`docs/COS切换后全链路测试-2026-09-14.log`

COS 应用级验证保留数据：素材 ID `52`，对象键 `uploads/1/cos-switch-validation-1789352243099.png`，签名下载 HTTP 200、内容逐字节一致。

## 5. 未纳入“已完成”的外部依赖

以下项目代码已有或已预留，但本地无法代表生产真实验收：

1. 微信支付真实下单、支付和回调。
2. COS 基础读写、应用上传落库、私有签名播放和下载已于 2026-09-14 真实验证通过；仍需用业务大视频复核分片上传、弱网续传和长视频处理。
3. 真实 FFmpeg 生产素材、中文字体、libass、长视频和最终成片质量。
4. 微信订阅消息模板、用户授权和消息发送。
5. 腾讯云/阿里云短信账号、签名、模板和供应商适配器。
6. TTS 供应商、真机音频播放和字幕渲染效果。
7. 微信开发者工具、真机兼容性、合法域名、审核和上线提交。
8. 甲方提供的镜头库示范视频、门店/菜品真实视频素材。

## 6. 代码回归结果

- `server`：`npm run typecheck` 通过
- `server`：`npm run build` 通过
- `apps/mini`：`npm run typecheck` 通过
- Prisma schema：已同步本地数据库并重新生成客户端
- 全链路脚本：全部通过，测试数据已保留

## 7. 本轮结论

除抖音端及上述需要甲方账号、真实第三方服务或真实素材的事项外，微信端核心业务链路已经能够在本地环境闭环运行。生产发布前应依据第 5 节逐项完成外部环境验收，并将最终源码、构建包、部署文档、接口说明、视频处理说明、第三方依赖清单和测试账号整理为交付包。
