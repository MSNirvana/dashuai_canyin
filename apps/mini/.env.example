# 复制为 .env 后按需修改（.env / .env.local 均不进 git）
#
# 接口基地址，会作为 __API_BASE_URL__ 编译进产物。优先级：
#   命令行 TARO_APP_API_BASE_URL  >  .env.local  >  .env  >  内置默认值
#
# ⚠ 构建脚本不再硬编码地址，所以**这个文件是唯一的地址来源**：
#     npm run dev:weapp         → 用本文件的值
#     npm run dev:weapp:local   → 强制连 http://127.0.0.1:3000（本地联调快捷方式）
#     npm run build:weapp       → 用本文件的值
#   （历史上 dev:weapp 里写死过 TARO_APP_API_BASE_URL，命令行优先级最高，
#     导致改了本文件却不生效 —— 已移除，别再往 package.json 里加回地址。）
#
# 本地联调（模拟器）：
TARO_APP_API_BASE_URL=http://127.0.0.1:3000/api/v1
#
# 真机调试：换成电脑局域网 IP，并在微信开发者工具勾选「不校验合法域名」：
# TARO_APP_API_BASE_URL=http://192.168.1.23:3000/api/v1
#
# 远程测试服务器（备案未过时用 IP 直连绕过接入层拦截，
# 原理与限制见仓库 deploy/开发测试连通-2026-09-15.md）：
# TARO_APP_API_BASE_URL=http://49.232.243.241/api/v1
#
# 正式环境：必须是已备案的 https 域名，且在小程序后台配置为 request 合法域名：
# TARO_APP_API_BASE_URL=https://api.dspcz.top/api/v1
