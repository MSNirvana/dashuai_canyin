# 微信支付配置清单（v3 / JSAPI）

> 依据：`server/src/lib/config.ts:48`（七项凭据校验）、`server/src/lib/wxpay.ts`（实际使用方式）。
>
> **本商户**：`1750405417` / 主体「廊坊大帅餐饮管理有限公司」/ 小程序 AppID `wx3004f53da3e30a36`
> —— 验签凭据形态为 **「微信支付公钥」**（非平台证书），见第五节的实证。
>
> **当前进度**：七项已填 6 项，只差微信支付公钥 PEM。**状态**：`PAYMENTS_ENABLED=false`
> （等域名备案通过）。

---

## 一、代码要求：七项 + 一个开关

生产环境启动时的硬校验（`validateProductionConfig`）：

| # | 环境变量 | 内容 | 校验条件 |
|---|---|---|---|
| 1 | `WX_PAY_MCH_ID` | 微信支付**商户号** | 非空 |
| 2 | `WX_PAY_API_KEY_V3` | **APIv3 密钥** | 非空 **且长度必须正好 32** |
| 3 | `WX_PAY_SERIAL_NO` | **商户 API 证书序列号** | 非空 |
| 4 | `WX_PAY_PRIVATE_KEY` | **商户 API 私钥**（apiclient_key.pem 内容） | 非空；`wxpayEnabled` 还要求含 `BEGIN` |
| 5 | `WX_APPID` | 小程序 AppID | 非空 —— **已有**：`wx3004f53da3e30a36` |
| 6 | `WX_PAY_NOTIFY_URL` | 支付回调地址 | 非空 |
| 7 | `WX_PAY_PLATFORM_CERT` | **微信侧验签凭据** —— 平台证书 **或** 微信支付公钥 | 非空；必须含 `BEGIN CERTIFICATE` 或 `BEGIN PUBLIC KEY` |

另加：

| 变量 | 值 | 说明 |
|---|---|---|
| `PAYMENTS_ENABLED` | `true`（或删掉这行） | 现在服务器上是 `false`，**必须改**，否则下单被拒（业务码 3008） |
| `PAYMENT_MODE` | `real` | 生产下 `paymentsEnabled=true` 时**强制**要求 `real`，不写会拒绝启动 |

> **格式陷阱（第 4、7 项）**：这两个是多行 PEM，但 `.env` 里必须写成**单行**，
> 换行用字面 `\n` 表示 —— 代码里执行 `replace(/\\n/g, '\n')` 还原。
> 直接把 PEM 原样粘进 `.env` 会解析失败。

生成单行值：

```bash
awk 'NF {sub(/\r/, ""); printf "%s\\n", $0}' apiclient_key.pem
```

---

## 二、每项从哪拿

| # | 位置（微信支付商户平台 pay.weixin.qq.com） |
|---|---|
| 1 商户号 | 「账户中心 → 商户信息」里的 **商户号（mchid）**，10 位数字 |
| 2 APIv3 密钥 | 「账户中心 → **API安全** → APIv3密钥 → 设置」——**32 位**，由你自己设定，**只显示一次**，务必当场存下 |
| 3 序列号 + 4 私钥 | 「账户中心 → **API安全** → 申请API证书」→ 下载证书工具 → 生成 `apiclient_cert.pem` / **`apiclient_key.pem`** / 序列号。**私钥只能生成这一次，丢了要重签** |
| 7 微信侧验签凭据 | **公钥（本商户的情况）**：「账户中心 → API安全 → **微信支付公钥管理** → 查看/下载公钥」，直接拿到 `wxp_pub.pem`，无需任何工具。<br>**平台证书（另一种模式）**：不在网页上提供下载，需用商户私钥调 `GET /v3/certificates` 拉取再用 APIv3 密钥 AES-256-GCM 解密（官方工具 `CertificateDownloader.jar` 也是干这个）。本商户调该接口返回 404（无平台证书）。 |

---

## 三、商户平台侧还要做的（非代码，缺一个都付不了钱）

这几件事不在 `.env` 里，但**不做就报错**：

1. **开户成功** —— 注册 ≠ 可用。必须走完 提交资料 → 审核 → **账户验证**（法人扫脸或小额打款）
   → 签约 → 「开户成功」。未开户成功时下单会报商户未开通。
2. **关联 AppID（关键）** —— 「产品中心 → **AppID账号管理**」把小程序 `wx3004f53da3e30a36`
   关联进来。**未关联时下单直接报 `APPID_MCHID_NOT_MATCH`。** 这一步最容易漏。
3. **开通 JSAPI 支付** —— 「产品中心 → 支付产品」确认 **JSAPI 支付** 已开通。
4. **绑定结算银行卡** —— 「账户中心 → 结算账户」，否则收不了款。
5. **确认 `notify_url` 公网可达** —— 见下节。

---

## 四、阻断点：回调地址卡在备案

`WX_PAY_NOTIFY_URL` 的值必须是：

```
https://api.dspcz.top/api/v1/pay/notify
```

（路由已实现：`server/src/index.ts:54` 挂载 `/api/v1/pay`，`routes/pay.ts` 处理 `/notify`）

微信要求回调地址 **HTTPS + 公网可达**。而 `dspcz.top` 的接入备案还在管局审核中，
腾讯云会在接入层拦截域名请求 —— **所以支付的真实可用同样卡在备案上**，和 API 是同一个阻断。

不过**配置可以现在就填好**：服务器目前以 `PAYMENTS_ENABLED=false` 运行，
凭据填进去不影响启动（那一行没改成 `true` 就不会触发七项校验）。
备案一过，改一个 `PAYMENTS_ENABLED=true` + 重启即可，不用等。

---

## 五、⚠ 需要你确认的一个技术分叉

### ✅ 已确认（2026-09-15）：本商户是「微信支付公钥」模式

原先代码只接受平台证书：`platformCert.includes('BEGIN CERTIFICATE')`。
实测发现本商户走的是**公钥模式**，该条件永远为假 → `wxpayEnabled` 静默变成 `false`
（服务器照常启动、下单全被拒、回调验签全失败）。**已修复**，两种模式现在都支持。

**怎么发现的**：用商户 API 证书签名调 `GET /v3/certificates`，返回

```
404 RESOURCE_NOT_EXISTS
无可用的平台证书，请在商户平台-API安全申请使用微信支付公钥。
```

> 附带两个副产品：
> 1. 这个 404 而**不是** `SIGN_ERROR`，说明**商户 API 证书签名是成功的** ——
>    商户号 / 序列号 / 私钥三者配套无误。
> 2. Node 的 `fetch`/undici 默认**不发 `Accept-Language`**，而微信支付 v3 会因此返回
>    一个极具误导性的 `406 PARAM_ERROR 传入了不支持的Accept-Language`。
>    排查时请**显式带上** `Accept-Language: zh-CN`，否则会顺着假错误白找半天。

### 两种模式的关系（务必分清，很容易搞混）

| | 商户 API 证书 | 微信支付平台证书 / 微信支付公钥 |
|---|---|---|
| 代表身份 | **商户侧** | **微信侧** |
| 用途 | 我们对微信**请求**的**签名** | 验证微信**下发**的回调与响应**签名** |
| 环境变量 | `WX_PAY_PRIVATE_KEY` + `WX_PAY_SERIAL_NO` | `WX_PAY_PLATFORM_CERT` |
| 来源 | API安全 → 申请API证书（zip） | API安全 → 微信支付公钥管理 / 平台证书 |
| 有效期 | 5 年 | 平台证书 5 年（需轮换）；**公钥不过期** |

平台证书与微信支付公钥**二选一**，功能完全等价 —— 微信 2024 年底推出公钥是为了
免去「证书会过期、要定期调接口轮换」的运维负担，因此**新商户默认只有公钥**。

### 代码侧改动

- `src/lib/wxpay.ts`：`wxpayEnabled` 改为接受两种形态（新增 `wechatVerifyMode()` 判断）；
  `verifyNotify()` 无需分叉 —— `crypto.verify()` 对 X.509 证书 PEM 和 SPKI 公钥 PEM 都支持。
  另为可测试性给 `verifyNotify()` / `wechatVerifyMode()` 加了可注入的凭据参数。
- `src/lib/config.ts`：生产校验加了一句 —— 验签凭据必须含 `BEGIN CERTIFICATE` 或
  `BEGIN PUBLIC KEY`，否则直接拒绝启动。原先只查非空，填错内容会**静默**降级。
- 契约测试：`npm run wxpay:verify`（`scripts/verify-wxpay-notify-signature.ts`），
  23 条断言覆盖两种模式的验签矩阵（篡改 / 伪造 / 过期 / 缺头 / 空材料全部拒绝）
  + 子进程验证真实 `wxpayEnabled` 常量在公钥模式下为 `true`。

> `Wechatpay-Serial` 头**刻意不强制校验**：本商户只配一把微信侧钥匙，没有歧义；
> 只要签名能过就是微信发的。而微信在「证书 → 公钥」灰度期该头可能仍返回平台证书序列号，
> 强校验会把合法回调误判为伪造 —— 那才是真事故（用户付了钱拿不到积分）。

---

## 六、还需要的输入

**现状**：服务器 `.env` 里 7 个键都在，已填 5 个：

| 键 | 状态 |
|---|---|
| `WX_APPID` | ✅ 已填 |
| `WX_PAY_MCH_ID` | ✅ 已填 `1750405417` |
| `WX_PAY_API_KEY_V3` | ✅ 已填（32 位，由我生成，商户平台需填同一个值） |
| `WX_PAY_NOTIFY_URL` | ✅ 已填 |
| `WX_PAY_SERIAL_NO` | ✅ 已填（从 `apiclient_cert.pem` 用 openssl 读出） |
| `WX_PAY_PRIVATE_KEY` | ✅ 已填（已转单行转义格式） |
| `WX_PAY_PLATFORM_CERT` | ⬜ **待填：微信支付公钥 PEM** |

**还需要你提供 1 样**：

- **微信支付公钥文件**（`wxp_pub.pem`）—— 商户平台「账户中心 → **API安全** →
  **微信支付公钥管理** → 查看/下载公钥」。顺带记下同页的**公钥 ID**（`PUB_KEY_ID_01...`）。

拿到后我会：

1. 转成 `.env` 要求的单行转义格式并写入配置
2. **保持 `PAYMENTS_ENABLED=false` 启动验证**（不改变现有可用性），确认 `wxpayEnabled` 为 true
3. 备案通过后：改 `PAYMENTS_ENABLED=true` → 重启 → 真机走一遍最低档下单

---

## 七、安全提醒

- **`WX_PAY_PRIVATE_KEY` 和 `WX_PAY_API_KEY_V3` 是能签发支付请求的凭据**，
  泄露等于别人可以用你的商户号下单/退款。只放在服务器 `.env`（600）与
  `~/.dashuai-deploy/`（仓库外，不进 git）。
- APIv3 密钥商户平台只显示一次；私钥只能生成一次。**先各自存一份到密码管理器再继续。**
- 本文档只写变量名与获取路径，**不含任何真实凭据**。
