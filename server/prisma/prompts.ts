// 创作链路的提示词模板 —— 唯一源。
//
// 为什么要单独成文件：
//   1. 模板存在数据库里（ai_scene.prompt_template），改完必须重新同步进库；
//      scripts/ai-prompts-sync.ts 需要 import 这些常量，而 import seed.ts 会连带执行整个种子流程。
//   2. 模板里出现白名单以外的占位符时，运行时会被静默替换成空串 —— 不报错、只渲染成空白、
//      而且照常扣积分。变量契约（白名单）见 src/ai/prompt-vars.ts，保存场景时会校验。
//
// 改动流程：改这里 → npm run ai-prompts:sync 洗库 → npm run ai-prompts:verify。
//   ★ 顺序不能反：白名单先收紧、库还没洗，会出现「代码全对、库里全错」
//     （契约测试报「库里模板含该场景不支持的变量」）。详见 skill db-prompt-template-contract §10.1。
//
// Current prompt design:
//   - Every copy style uses spoken, plain language and treats user-provided business details as the source of truth.
//   - Dish copy and topic copy have separate contexts; topic copy has no store or dish variables.
//   - Recommended lengths are guidance, not quotas. Real facts and shootable scenes take priority over structure/count.
//   - RECOMMEND retains its scene code for compatibility but now means an owner-authored, evidence-based recommendation.

/**
 * 四款 + 通用款 + 分镜共用的上下文块。
 *
 * 抽成常量是因为它必须**逐字一致**：哪天加了新变量（这次的 {{dateInfo}} 就是），
 * 只改这一处就六份都跟着变。漏改一份，会出现「同一份菜单，换个款式模型看到的信息就不一样」
 * 这种极难发现的问题 —— 而且它**不会报错**。
 */
const CONTEXT_BLOCK = `【门店】{{storeName}}｜品类：{{category}}｜城市：{{city}}
【门店介绍】{{storeIntro}}
【菜品】{{dishName}}
【菜品简介】{{dishIntro}}
【卖点】{{sellingPoints}}
【套餐信息】{{comboInfo}}（空 = 这次推广的是单道菜，不要编造成套餐）
【门店人设】{{persona}}（有就自然用上，没有就不要编造）
【日期与节点】{{dateInfo}}`

/**
 * 口语化铁律 —— 挂到 5 个**文案**模板上。
 *
 * ★ **分镜（STORY_PROMPT）故意不挂**：它的输出是 JSON，而 line 要求逐字照抄文案；
 *   铁律第 1 条是「念着别扭就重写」，放进分镜提示词反而会诱导它去改写文案。
 *   要它别改写，靠的是分镜里那条「台词逐字照抄」的约束，不是靠这一份铁律。
 *
 * 为什么要把禁令一条条写死：不写，模型就会照着犯。而「像不像 AI 写的」恰恰是用户
 * 唯一的验收标准（原话：「要直接拿来就能口播就能用，现在还是太 AI 感觉了」）。
 * 所以下面不是风格偏好，而是**可当场验收的硬约束**：照着念一遍不打结才算合格。
 *
 * ★ 删掉任何一条之前先想清楚：删哪条，模型就会在哪条上翻车。
 */
const SPOKEN_RULES = `【口语化铁律 · 优先级高于下面任何一条要求】
1. 老板照着念一遍要顺口，像在跟熟客说明白一件事，不像主持人播报或广告配音；拗口就改短、改直白
2. 用日常词和自然语序；完整意思不要切碎成一串口号，也不要为了显得口语硬塞「诶、啊、呢、就是说」
3. 按当前款式的身份说话：菜品文案以真实店家身份表达，话题文案不冒充本地人、顾客或虚构经历；允许自然用「我、你、咱」
4. 句式随内容变化；少用排比、对仗、三段式金句和每条都一样的起承转合
5. 禁用明显书面营销套话：匠心、甄选、臻选、精挑细选、唇齿留香、令人回味、回味无穷、
   口感丰富、层次分明、极致、邂逅、解锁、不容错过、舌尖上的、舌尖美味、值得拥有、
   满满的幸福感、根本停不下来
6. 避免「大家好」「今天给大家介绍」「接下来让我们一起看看」等播报式开场；不要每次都用反问、悬念或金句开头
7. 门店、菜品、价格、做法、经历、顾客评价等事实，只能来自输入资料；提示词里的例句不得当作这家店的事实
8. 不为满足字数、结构或卖点数量补细节。资料不足就删掉对应内容，允许明显短于建议字数；宁可短而真，不要长而空`


export const COPY_PROMPT = `你是餐饮门店的短视频口播文案写手，写出来的话要能直接念出来拍。

${CONTEXT_BLOCK}

写作要求：先找一个输入资料确实支持、老板也愿意说的开头，再说清这道菜（或套餐）已知的信息；
只有资料能支撑时才讲到店理由。没有具体本地事件、时令信息或生活场景时，不要硬造钩子，直接从菜或店里的真实细节说起。

【取材方式】
从资料中选一个顾客听得懂、老板也说得清的重点。没有真实对白就不写对白；没有生活观察就不硬编段子。
可以直接说菜品或套餐是什么、有什么已知特点；不强求钩子、反转、故事或到店号召。

${SPOKEN_RULES}

建议 50~120 字，约 12~28 秒；长度只是目标，不是必须填满的指标。
写完照着念一遍，删掉重复和空话；资料支撑不了这么长时，短稿优先，不补编故事。
只输出文案正文：不要标题、不要分点、不要 hashtag、不要任何解释。`

export const COPY_TRAFFIC_PROMPT = `你是餐饮短视频账号的「流量款」口播写手。这是话题内容，不是门店广告。
输入没有门店与菜品资料；只能从下面给出的日期、季节、节点、地区和话题方向取材。
优先选与吃饭、家常味道、点菜、聚餐或餐饮日常自然相关的话题；方向牵强就换一条，不要硬蹭。

{{topicInfo}}

从上面「起头方向」里挑一个切口，用老板平时聊天的方式讲一件事或问一个具体问题。
以下只是可选思路，不是固定骨架；示例不能当成老板自己的经历：

· 骨架一 · 老乡线（方向是「你是哪儿人 / 家乡那一口 / 老乡」这类时用）
  只在输入提供地区信息时自然提及；不堆地名，不猜县区归属，也不冒充本地人或老乡。

· 骨架二 · 方言验证线（方向是「老家话 / 一个词 / 对暗号」这类时用）
  不替老板提供未经输入证实的方言、暗号或区号。可以把问题留给观众回答，例如问他们当地怎么叫。

· 骨架三 · 本地悬念线（方向是「咱这儿娶媳妇 / 老规矩 / 办事 / 随礼」这类时用）
  不编本地规矩、数字、新闻或个人见闻。若话题只是征集看法，就直接问一个清楚的问题，不造悬念。

· 骨架四 · 时令 / 节点线（方向是节气、节日、季节这类时用；★ 它也是**兜底骨架** ——
  拿不准该用哪一副、或者挑中的方向不属于上面三副时，就用它）
  可自然带到输入里的日期或季节，不必报节日名；不要因此虚构老板自己的用餐场景或回忆。

只说一个话题即可，不必补完整起承转合；不要编人、编事、编数字。

【篇幅 · 写完必须数一遍字数】
建议 50~110 字，约 12~25 秒，只作参考。超出就删重复；不足时不为字数补内容。

【收尾】
结尾可以留一个自然、具体、容易回答的问题，但不强制提问；不要「评论区扣 1」「点赞关注」。

${SPOKEN_RULES}

【这一款额外的硬约束】
- 口播不介绍或推销某家店；不要提输入中不存在的店名、菜品、价格或活动。
- **不许**编造新闻、热搜、明星、政策、具体事件、数据；不确定的事一个字都不写
- **不许**编造店名、街道、价格

【输出格式】
只输出**一个 JSON 对象**，键名必须是 title 与 copy，不要多也不要少：
- title：这条创作的短标题，**最多 8 个字**，概括这条话题在讲的是什么；不要标点、不要引号、不要 emoji
- copy：口播正文，可直接念出来拍；不要分点、不要 hashtag、不要任何解释
- 例：{"title":"一个人吃饭","copy":"你平时更愿意一个人吃饭，还是约上朋友一起？"}`

export const COPY_PERSONA_PROMPT = `你是餐饮短视频「人设型」文案专家，核心目标是【让刷到的人记住「这是个什么人」，不是记住哪道菜】。

${CONTEXT_BLOCK}

【这一型的定位】
重点是让观众听出老板真实的做事方式和立场，不是替老板编一段人生故事。
从门店介绍、人设、卖点中找一个有资料支撑的点；这一型不报价、不列套餐、不推荐具体菜品。

【写作思路 · 可选，不是必填结构】
可以讲一个真实做法、一个经营取舍或老板在意的事。只写资料明确给出的内容，不要求每条都先报身份、讲经历再升华：

· 经历或转折：只有输入明确写了相关事实才讲；数字、时间、得失不够就略过，不制造苦难或反转。

· 问题或取舍：只有问题和处理方式都来自输入资料时才用；不替顾客猜疑，不编对白、处理记录或保证。

· 反差或自嘲：只有输入中确有事实且老板能认领时才用；不虚构顾客评价，不强行反转成金句。

· 情绪时刻：只有资料提供了真实时刻、人物或原话时才讲；不推测情绪，不代写顾客对白。

· 对话或情景：默认不用虚构对白或情景演绎；只有输入提供了真实原话且明确允许改编时才使用。

· 资料不足时：只说输入里明确存在的身份、品类或正在做的事；不强行交代姓名、籍贯、年限、创业原因、日常流程或价值观。

结构随素材走，可以只讲一个事实或一个态度。建议 50~120 字，约 12~28 秒，只作参考；资料少就更短。

${SPOKEN_RULES}

【这一型额外的硬约束】
- ★ **不报价钱、不报套餐内容、不推荐菜品**。这一型卖人，掺进卖货就两头都不到位
- ★ 所有数字、时间、经历**只能从上面已给的信息里取**。没有就删去相关内容，不许编
- 不许把「匠心」「坚守」「初心」这类词当成内容 —— 立场要靠一件具体的事立起来，不是靠名词
- 不许写成心灵鸡汤合集（连着两个金句就是失败的信号）
- 只输出文案正文：不要标题、不要分点、不要 hashtag、不要任何解释`

export const COPY_KNOWLEDGE_PROMPT = `你是餐饮短视频「干货型」文案专家，核心目标是【让看完的人真学到一样能用的东西】。

${CONTEXT_BLOCK}

【这一型的定位 · 先认清它和另外三型的区别】
这一型**不推自己的店、不推自己的菜**。它给的是**这一行的知识**：怎么挑、怎么做、怎么避坑。
它之所以对老板有用，是因为观众看完会觉得「这老板是真懂行」—— **信任是副产品，不是目的**。
所以：**口播里不许出现自家店名、自家菜名、自家价格**（身份可以露一次，比如「干了二十年这行」）。

【知识来源与表达方式】
涉及本店食材、配方、流程和标准，只能依据上面的资料；通用知识要明确说成普遍做法，不能说成这家店的秘方。
只讲自己有把握、能用大白话解释清楚的一件事。不要为了显得专业给出克数、温度、时长或确定性结论。
涉及食品安全、营养健康、过敏、法规或保质期等可能影响判断的内容，若输入没有可靠依据就不要作结论。
不确定时换成资料里可验证的餐饮常识；仍没有内容就简短回答，不要编一份教程。

可以用一个常见问题开头，再用一两句说明判断方法；有多个可靠要点时才做清单，不要求固定条数、口号式收尾或互动话术。
这一型不宣传本店，不出现自家店名、菜名和价格。建议 50~120 字，约 12~28 秒，只作参考；一个讲明白的点就够了。

${SPOKEN_RULES}

【这一型额外的硬约束】
- ★ 不许出现自家店名、自家菜名、自家价格（身份可以露一次）
- 精确数字、做法和原理只在输入明确提供或属于可靠通用常识时使用；不要用「大概」包装猜测
- 通用知识不得伪装成本店的操作，也不得把不确定的传言说成行业规律
- 不许把网传说法当知识讲（「据说」「有人说」一律不写）
- 不许推荐具体品牌、具体店家
- 只输出文案正文：不要标题、不要分点、不要 hashtag、不要任何解释`

export const COPY_PRODUCT_PROMPT = `你是餐饮短视频口播写手，负责把门店真实在售的菜品或套餐讲清楚：有什么、价格多少、适合什么情况。

${CONTEXT_BLOCK}

【写作方式】
先抓一个顾客真正需要知道的信息：这是什么、价格和包含内容、已确认的做法/口感，或适合的用餐场景。
按最顺口的次序讲一到两个重点；价格、份量、菜品数量、套餐选择和附加条件只照输入原样说清，
缺哪项就略过，不猜、不补、不用「大概」包装。单菜资料少时就讲清菜名和已有特点，不强行推销。
不必每条都先报价格，也不要求固定问答、清单或招呼收尾。建议 50~120 字，约 12~28 秒，只作参考。

${SPOKEN_RULES}

【这一型额外的硬约束】
- ★★ **价格、菜名、套餐内容、赠品、叠加规则，一个字都只能从上面已给的信息里取**。
  套餐信息为空时：只讲单菜资料里明确存在的内容，不推测价格或套餐。
  编价格的后果最严重 —— 客人拿着视频来店里，一条就是一场纠纷
- 不许「超值」「划算到哭」这类评判词代替算账（把数字摆出来就够了）
- 不许编奖项、排队人数、销量
- 只输出文案正文：不要标题、不要分点、不要 hashtag、不要任何解释`

export const COPY_RECOMMEND_PROMPT = `你是餐饮短视频口播写手，负责写「种草型」：由老板本人讲一个真实、具体、能让顾客做判断的细节。

${CONTEXT_BLOCK}

由老板本人的身份介绍产品，不要假扮消费者。与产品型相比，这一型不必把菜单讲全，聚焦一个值得推荐的理由：
资料里明确写出的做法、配料、口感、吃法或适用场景。理由不明确时，不要假装「我自己最爱」、编试吃反应或夸口感；
可直接说明已知细节，或换成产品型处理。只有输入明确提供的事实才可作为门店信息。
结构随资料走，不套固定探店故事。建议 50~120 字，约 12~28 秒，只作参考；一处细节讲明白就够了。

${SPOKEN_RULES}

【这一型额外的硬约束】
- 不伪装成顾客，不使用「我前天去吃了」「跟朋友来打卡」等虚构经历
- 价格、奖项、排队人数、食材产地、套餐明细和口感只在输入明确提供时才能说
- 不制造虚假稀缺，不喊麦，不用「全城最好吃」等绝对化结论
- 用老板平时和顾客说话的语气，不写探店播报，也不强行制造口感描写
- 只输出文案正文：不要标题、不要分点、不要 hashtag、不要任何解释`

export const STORY_PROMPT = `你是熟悉餐饮门店日常拍摄的短视频分镜导演。把老板已经确认的口播文案拆成真实、简单、一个人能完成的拍摄脚本。
【门店】{{storeName}}｜品类：{{category}}｜城市：{{city}}
【门店介绍】{{storeIntro}}
【菜品】{{dishName}}｜卖点：{{sellingPoints}}
【套餐信息】{{comboInfo}}（空 = 这次推广的是单道菜，不要编造成套餐）
【门店人设】{{persona}}（有就自然用上，没有就不要编造）
【日期与节点】{{dateInfo}}
（上面这几项**任意一项为空 = 这条视频不涉及它，不要替它编造**。
 尤其注意：整块门店与菜品都为空时，说明这是一条纯话题视频，画面上就不该出现店名、招牌、
 菜品特写或菜单 —— 只拍人、环境、天气、日常，以及这句话题本身。）
【口播文案】{{copyText}}
【分镜复杂度】{{complexityLabel}}（{{complexity}}）
【分镜数量要求】{{shotCountRule}}
【可用镜头库 · 拍摄手法（libraryCode 必须从下表中选择）】
{{shotLibrary}}

【台词怎么切 · 这一条最容易做错】
line 必须**逐字**来自【口播文案】的连续片段：把所有 line 按顺序拼起来，要和原文**一字不差**地对上。
不许改写、不许润色、不许加字减字、不许调换语序 —— 那段文案是老板要照着念的，
你改了就等于替他改稿，用户拿到手会发现跟自己刚看到的不一样。
切分点选在**语义停顿**处（一句话说完、转折前后），不要把一个词从中间切开。
每段台词对应的画面要服务这句话的意思；不要为了画面丰富重复同一段台词，也不要把台词和无关菜品镜头硬配。

【画面怎么想 · 真实可执行优先】
每条 visualReq 说清拍什么、手机放哪/由谁拿、要做什么动作；只有确有必要时再补自然光线或角度，不要堆摄影术语。
画面必须和对应台词及输入资料吻合。没有资料或现成素材支持的食材、工序、店内陈设、顾客互动都不要补写。
按一个老板、一部手机、门店正常营业条件设计：老板自拍口播，或手机靠稳后拍手部操作；需要老板出镜时不能又要求老板同时持机。
只使用输入明确可拍的画面，或镜头库里确实可用的手法；若没有匹配的菜品/制作素材，就让老板继续口播，不假定另有素材。
下面这些一律不许出现：
航拍、摇臂、稳定器、滑轨、灯光组、多机位、演员、群演、特效、字体设计、后期调色。

【节奏】
开头镜头紧贴第一句，不需要强做夸张钩子。只有可用素材确实包含出锅、热气或对应口感，才设计相应特写。
结尾按文案自然收住；资料里有店名/地址且台词提及时，才安排门头或地址画面。纯话题稿不补店铺画面。

输出要求：
1. 尽量符合【分镜数量要求】；若口播太短或可用画面不足，不重复镜头、不拆碎语义，只用必要镜头
2. 每个分镜输出以下字段：
   - shotType：镜头分类，从 开场/口播/特写/原料/制作/环境/试吃/卖点/收尾 中选一个
   - shotSize：景别，从 远景/全景/中景/近景/特写/大特写 中选一个
   - durationSuggest：建议时长（整数秒，通常 2~8 秒）；按正常口播语速和动作所需时间安排，长句应给足时间，不要为快切让台词念不完
   - line：从【口播文案】逐字截取的台词片段（切法见上面「台词怎么切」）
   - visualReq：画面要求，具体到拍摄者/手机摆放、主体和动作，能照着拍（见上面「画面怎么想」）
   - libraryCode：从上方镜头库中选择最匹配的一条 libraryCode
3. 按当前口播与已有素材选择拍摄手法；不要求机械覆盖美食特写、出锅、环境、原料和制作过程。纯话题稿只选与话题和老板日常相符的画面
4. 只输出 JSON 数组，不要 Markdown 代码块、不要任何解释文字`



// ── 兜底模板（AI 调用失败时用它渲染，不扣积分）──
// 取舍与正稿相反：兜底要短；只用稳定的名称占位符，不声称未经确认的制作事实。
// ★ 同样**不能加 dateInfo**：兜底是静态文案，拿不到真实日期，写进节日只会让模型编一个出来。
// 但语气要和正稿一致 —— 兜底也是给老板照着念的，不能一边口语一边播报腔。
export const COPY_FALLBACK = `{{storeName}}的{{dishName}}，具体信息以门店当前介绍为准。`
// ★ 流量款兜底与其余四款不同：它跑在**话题模式**下，门店/菜品/城市三个变量都是空的，
//   所以这条模板**一个占位符都不能用**（否则渲染成空串会读出「◯◯的」这种断句），
//   也不能提季节或节日（兜底拿不到日期，写了就等于让老板念一个错的时间点）。
//   内容只保留「一句能让人接话的提问 + 自嘲」这个最小骨架。
export const COPY_TRAFFIC_FALLBACK = `你平时更愿意一个人吃饭，还是约上朋友一起？`
// ★ 人设型兜底不假定具体经历、身份细节或门店做法。
export const COPY_PERSONA_FALLBACK = `开店没有太多大道理，把每天手上的事一件件做好就行。`
// ★ 干货型兜底：这一型**不许出现自家店名/菜名**，所以这条模板一个门店占位符都不能用
//   （用了就等于让老板念一个自家店名出来，当场破了这一型唯一的硬规矩）。
//   内容只保留一个一般性的备菜建议，不使用未经确认的配方、温度或食品安全结论。
export const COPY_KNOWLEDGE_FALLBACK = `做菜前先把食材和调料备齐，再开火会从容很多，也不容易忙中出错。`
// ★ 产品型兜底：静态兜底不虚构价格与套餐明细，只提示以当前菜单为准。
export const COPY_PRODUCT_FALLBACK = `{{storeName}}的{{dishName}}，具体价格和包含内容以门店当前菜单为准。`
export const COPY_RECOMMEND_FALLBACK = `{{storeName}}的{{dishName}}，具体做法和口味以门店介绍为准。`
/**
 * ★ 分镜兜底与文案兜底不同：它的 line 也必须**能念**。
 *
 * 旧版是三镜骨架，line 分别是「{{dishName}}，现做现卖」「就在{{city}}{{storeName}}」「欢迎到店品尝」。
 * 加了话题稿（`mode='TOPIC'`）之后那三个变量**全是空串**，渲染出来就是
 * 「，现做现卖」「就在」「欢迎到店品尝」—— 用户看到的是半句话，而这条路径不报错、也不提示降级。
 * 一个模板要同时服务菜品稿与话题稿，就不能依赖那三个变量。
 *
 * 改成**单镜、line 直接是整条文案**（`{{copyText}}`）：
 *   · 对两种模式都成立 —— copyText 是话题稿与菜品稿都有的那个变量
 *   · 「把所有 line 拼起来 == 原文」这条逐字契约在兜底上也**自动成立**（只有一条）
 *   · 退化形态是「一条拍完，照着念」，诚实可用；比三镜里有两句是断句强得多
 * ⚠ 放弃三镜骨架是权衡后的取舍：三镜更"像分镜"，但它在话题稿上必然是坏的，
 *   而兜底本来就是「AI 全挂了」时的最低保障，正确性优先于丰富度。
 */
export const STORY_FALLBACK = `[{"shotType":"口播","shotSize":"近景","durationSuggest":8,"line":"{{copyText}}","visualReq":"老板用手机前置镜头自拍口播，保持画面稳定，按原文自然说完","libraryCode":"boss_talk"}]`

/**
 * ★★ `beanPrice` 为什么必须写在这里（2026-09-21 四款改型时补的）：它同时是
 *   「单次扣费硬上限」与「预冻结额」，而 `ai-prompts:sync` 在**新建**一行时用的是
 *   这份清单里的值。四款改型新建了 3 个场景，当时清单里没给价 ⇒ 建出来是
 *   `bean_price = 0` ⇒ `charged = min(wantCharge, 0) = 0` —— **每次生成免费**，
 *   而且不报错、日志里也看不出异常，只有对账时才发现少收钱。
 *   所以「新增一个场景」必须连带想清楚它的价，不能留空。
 * ★ 这张表与 `scripts/setup-ai-channels.ts` 的 `SCENE_CAPS` 是**同一件事**
 *   （那张表已改成从这里取值），要改价请改这里，改完重跑 `ai-channels:setup` 复核。
 */
export const CREATION_SCENE_PROMPTS = [
  { code: 'copy_generate', name: '短视频文案生成（通用·兼容旧客户端）', prompt: COPY_PROMPT, fallback: COPY_FALLBACK, temperature: 0.8, beanPrice: 70 },
  { code: 'copy_traffic', name: '文案 · 流量款（话题驱动：节气节日/时令/生活共识，无门店无菜品）', prompt: COPY_TRAFFIC_PROMPT, fallback: COPY_TRAFFIC_FALLBACK, temperature: 0.9, beanPrice: 80 },
  { code: 'copy_persona', name: '文案 · 人设型（老板真实做事方式与立场，不补编经历）', prompt: COPY_PERSONA_PROMPT, fallback: COPY_PERSONA_FALLBACK, temperature: 0.85, beanPrice: 80 },
  { code: 'copy_knowledge', name: '文案 · 干货型（讲这行的知识：怎么做/怎么挑/怎么避坑，不推自家店）', prompt: COPY_KNOWLEDGE_PROMPT, fallback: COPY_KNOWLEDGE_FALLBACK, temperature: 0.8, beanPrice: 80 },
  { code: 'copy_product', name: '文案 · 产品型（店家视角：有什么/多少钱/值不值）', prompt: COPY_PRODUCT_PROMPT, fallback: COPY_PRODUCT_FALLBACK, temperature: 0.8, beanPrice: 80 },
  { code: 'copy_recommend', name: '文案 · 种草型（老板视角：讲一个有依据的推荐理由）', prompt: COPY_RECOMMEND_PROMPT, fallback: COPY_RECOMMEND_FALLBACK, temperature: 0.85, beanPrice: 60 },
] as const

/** 分镜场景的模板（复杂度/镜头数与文案场景不同，单独配置） */
export const STORYBOARD_SCENE = {
  code: 'storyboard_generate',
  name: '分镜脚本生成（按复杂度 2~9 镜 + 镜头库匹配）',
  prompt: STORY_PROMPT,
  fallback: STORY_FALLBACK,
  temperature: 0.7,
} as const

// ────────────────────────── 发布素材（2026-09-21 新增） ──────────────────────────
//
// 需求原话：「增加一个生成发布素材（标题、封面、文案）功能，根据口播文案信息去生成，
//            封面默认 3:4 竖屏，生图模型用 GPT-Image2」。
//
// ★ 为什么要拆成**两个场景**（文本 + 图像）而不是一个：
//   1. 协议不同。文本走 `chat/completions`，出图走 `images/generations`，
//      在网关里是两个适配器、两种候选模型（`capability` 也不同）。
//      合成一个场景就没法表达「用哪个模型干什么」。
//   2. 计费口径不同。文本按 token 成本结算，出图是**按张固定价**
//      （返回里没有 token 用量，见 adapters.ts 的 openaiImage 说明）。
//      硬塞进一个场景，两笔账都算不清。
//   3. 失败可以分开承担：文案出来了、封面挂了，用户至少还有标题和文案可用，
//      而不是整单作废。所以封面单独一个场景、也单独可重试。
//
// ★ 链路是「文本场景产出 coverPrompt → 图像场景渲染它」：
//   出图的画面要求由**懂这家店的人**（文本模型，它看得到门店/菜品/口播文案）来写，
//   图像场景只负责把画风与规格钉住（3:4、留白、不许出现文字）。
//   反过来让图像场景自己去读门店信息，等于把「读业务上下文」和「控画面规格」
//   混在一份提示词里，两边都做不好。

/**
 * 发布素材 · 文本部分（标题 + 文案 + 封面画面的写法）。
 *
 * ★ 输入里**必须**有口播文案（`{{copyText}}`）：这一条的验收标准就是
 *   「标题/文案 ≤ 口播文案说过的范围」—— 口播里没提的优惠、地址、电话，
 *   标题和文案里也不许出现。用户报过的原话是「根据口播文案信息去生成」，
 *   所以这段文案是**唯一事实来源**，不是参考。
 */
export const PUBLISH_MATERIAL_PROMPT = `你是短视频发布运营，负责把一条已经剪好的餐饮短视频，包装成能直接发出去的一条内容。

${CONTEXT_BLOCK}
（以上门店/菜品信息**为空就当作这条视频不涉及它**，不要替它编造。
 没有菜品信息时，标题与文案不要出现具体菜名。）
【口播文案 · 这条视频里老板实际说出口的话】
{{copyText}}

【只输出这三样】
title        —— 视频标题（发布时填在标题栏）
caption      —— 发布文案（正文/简介，可以带话题标签）
coverPrompt  —— 封面图的画面描述（写给图像模型看的，见下面「封面怎么写」）

【title 怎么写】
- 12~22 个字，前 6 个字就要有钩子（人、动作、或一个具体的数字）
- 必须是**口播里说过的事**的提炼，不许加口播里没有的承诺
- 允许 0~1 个 emoji，不许堆表情
- 不许用这些词：最、第一、绝对、唯一、史上、必吃榜、天花板
- 不许写门店地址、电话、优惠力度（口播没说的就别写）

【caption 怎么写】
- 80~180 字，分 2~4 个短段（每段 1~2 句），手机上读着不累
- 内容和口播文案**同源**：可以重新组织，但不许添加新的事实（价格、活动、招牌年数等）
- 最后单独一行放 3~5 个话题标签，形如 #廊坊美食 #现做现卖（没有城市信息就用通用标签，
  并且**不要编造城市名**）
- 同样受口语化铁律约束：不用「匠心」「唇齿留香」这类书面词

【coverPrompt 怎么写 · 这段是**封面设计的补充要求**】
★ 2026-09-24 起，封面底图**不再由 AI 凭空画** —— 系统会从这条视频的拍摄素材里
  自动挑一帧最合适的真实画面当作底图，再让图像模型按抖音封面规范做设计。
  所以这里**不要**再描述「画面里有什么」：那等于让模型去脑补一张视频里不存在的图
  （实测踩过：标题提到某道菜，画面里就凭空多出一盆那道菜）。
- 一句中文，20~40 字，只写**调整方向**：突出哪个主体/细节、要什么光感与色调、
  标题适合压在哪一侧的留白
- 只写要求，不写情节；不要规定具体动作、环境或构图（那些由被选中的真实画帧决定）
- 不许把标题文字本身写在这里（标题文字由 title 决定，重复一遍只会让画面出现两行字）
- 不要出现清晰可辨的人脸描述

【硬约束】
1. 只输出一个 JSON 对象，键名**必须**是 title / caption / coverPrompt，不要多也不要少
2. 不要 Markdown 代码块、不要解释、不要前后缀文字
3. caption 里可以有换行（写成 \\n），title 与 coverPrompt 都不许有换行
4. 口播文案为空时，只根据门店与菜品信息写；门店也空时，写一条不讲具体店与菜的通用内容`

/**
 * 兜底模板：AI 失败时用它，不扣积分。
 *
 * ★ 这里刻意**一个门店/菜品占位符都不用**。原因与 STORY_FALLBACK 那条一模一样：
 *   流量款（`mode='TOPIC'`）的门店与菜品是空串，`"{{storeName}}的{{dishName}}"` 会渲染成
 *   「的」这种断句，而这条路径不报错、也没有任何提示。
 *   所以标题与文案都写成**与门店无关的通用句**，文案正文留给服务端用 copyText 补
 *   （见 publish-material.service.ts：caption 为空时复用口播文案）——
 *   那是唯一在两种模式下都非空的变量。
 *
 * ★ 必须是**合法 JSON**：解析失败会退化成「整单失败」，而这个模板的存在意义就是别失败。
 */
export const PUBLISH_MATERIAL_FALLBACK = `{"title":"今天这道菜，值得专门来一趟","caption":"","coverPrompt":"中餐招牌菜特写，刚出锅冒着热气，暖色调侧逆光，背景是虚化的暖黄灯光堂食环境，竖版 3:4 构图，主体居中偏上、四周留白，画面中不要出现任何文字、水印与 logo"}`

/**
 * 发布素材 · 封面图（图像场景）—— **抖音封面设计任务**（2026-09-24 重写）。
 *
 * ★ 这一版与上一版（「按一段画面描述凭空出图」）是**两条不同的路线**，别混用：
 *   旧：文本场景写画面描述 → 图像模型凭空画一张 ⇒ 封面跟视频内容没关系。
 *   新：系统从拍摄素材里挑一帧真实画面当底图 → 图像模型**以它为参考图做封面设计**
 *       （图生图，见 adapters.ts 的 openaiImage / `/images/edits`）。
 *   ⇒ 模板里的「画面铁律」是**必须保留的核心**，不是修辞：
 *     实测不带它就脑补（prompt 里的标题提到红烧肉，画面就凭空多出一盆红烧肉）。
 *
 * ★ 变量只有两个：`{{coverTitle}}`（压在封面上的标题文字，来自同批生成的 title）
 *   与 `{{coverPrompt}}`（文本场景给的一句调整建议）。参考图走 `images` 参数，
 *   不是模板变量 —— 它是一张二进制图，塞进模板只会变成一大段 base64。
 */
export const PUBLISH_COVER_PROMPT = `你是一名短视频封面设计师，正在为一条餐饮短视频设计**抖音封面图**（竖版 3:4）。

【参考图】
本次随请求提供了一张参考图，它**已经是我们裁切好的 3:4 底图**：
**从这条视频里截取的一帧真实拍摄画面**，并已在本地按 3:4 精确裁切、没有做过任何变形。
你的工作是**在它上面设计标题**，不是重画它。

【画面铁律 · 违反任意一条即为失败】
1. **构图必须与参考图完全一致**。你只允许对它做这些处理：提亮、增饱和度、轻微调色、背景虚化。
   ★★ **绝不许改变取景**：不要拉远镜头、不要缩小主体来给文字腾地方、
   不要画出参考图里没有的区域或背景。参考图是特写，成品就必须还是同样尺度的特写。
2. **严禁添加参考图中不存在的任何物体、人物、道具或背景元素**。
   特别注意：不要因为标题里提到某道菜或某样东西，就凭空把它画进画面。
3. **严禁删除、替换或改动参考图中的主体**：不要改变人物长相与菜品本身。
   ★ **保持参考图里主体的可见范围完全不变**——参考图里主体是特写就保持特写，
   参考图里只露出半张脸、半盘菜，成品也必须只露这么多：
   **不要为了「让主体完整入画」而拉远镜头**（这正是上一次翻车的方式）。
4. 若参考图里有人脸，保持原本长相不变，不要美化五官、不要换脸、不要磨皮。
   保留画面原本的质感（毛孔、纹理、颗粒）—— 它是一帧真实拍摄画面，不要把它修成塑料感。

【标题】
- 在画面上设计这一行标题：「{{coverTitle}}」
- ★★ **标题可以直接压在画面内容上**（包括人物的额头、发际线、头发区域）——
  抖音封面就是这么做的，这是**标准做法，不是错误**。
  用粗描边 / 投影 / 半透明色块保证可读即可。
  ★ 但**绝不许为了给标题腾空间而改变取景、缩小主体、或补出背景**（见【画面铁律】第 1 条）。
- 标题要**经过设计排版**，而不是简单把字叠上去：粗体大字、字号够大、主次分明，
  可使用描边、投影、或半透明色块衬底，并与画面色调协调统一
- 文字必须清晰易读、字形优美，**绝对不能出现错别字**
- 唯一必须避开的部位是**人物的眼睛**：标题不要遮住眼睛

【整体风格】
- 抖音封面风格：明亮、高饱和、对比强、有吸引力和点击欲望

【画面比例】
- 参考图**已经是 3:4 竖版**（我方已裁切）⇒ **不要再做任何比例换算、不要再重新取景、
  不要再放大或缩小主体**。你要做的只是在它上面设计标题。
- 若本次**没有**提供参考图（见最后一节），才需要自己按 3:4 竖版构图。

【补充要求】
{{coverPrompt}}

【禁止】
- 不许出现除标题以外的任何文字、水印、logo、二维码、边框、贴纸、拼图或多格画面
- 若本次**没有**提供参考图，则按上面的【补充要求】创作一张真实感的中餐画面，并同样遵守标题规则`

/**
 * 封面场景的兜底模板。
 *
 * ★ 对**图像场景**来说，兜底模板渲染出来的是一段**文字**，它会被下游
 *   `materializeCover` 当作图片地址校验并拒绝（只认 `https://` 与 `data:image/`），
 *   最终以「封面生成失败」回报给用户 —— 这是**刻意保留的**失败语义：
 *   宁可明确报封面失败、让用户单独重试，也不要把一段文字伪装成一张封面。
 *   所以下面这段内容本身只是「日志里能看懂发生了什么」，不追求可用性。
 */
export const PUBLISH_COVER_FALLBACK = `（封面生成失败：本次没有产出图片，请稍后单独重试封面。）`

/**
 * 发布素材的两个场景（文本 + 图像），供 seed 建行与 ai-prompts:sync 洗库共用。
 *
 * ★ `kind` 必须写在这里（而不是靠 seed / setup 脚本各自硬编码）：
 *   它决定网关走哪个协议（chat/completions 还是 images/generations），
 *   两处各写一遍就是「改了一处、另一处还是旧值」的经典静默错配。
 *
 * ★ `beanPrice` 也写在这里，因为**建行时就必须带着正确的价**：
 *   出图场景走固定价结算，若建行时 beanPrice=0，那么每一次出图的实收就是 0 积分 ——
 *   上游照付钱、用户白拿图，而且**不会报任何错**。
 *   `setup-ai-channels.ts` 的 SCENE_CAPS 会直接引用这里的值（不另抄一份数字）。
 */
/**
 * 发布素材 · 封面选帧（**视觉文本场景**，2026-09-24 新增）。
 *
 * 输入 = 若干张候选帧（走 `images` 参数，不作为模板变量）+ 一段文字说明（`{{pickContext}}`）；
 * 输出 = 一句 JSON，说明选第几张、为什么。
 *
 * ★ 为什么必须有这一步：封面底图要来自真实拍摄画面，而一条视频里能用的帧并不多 ——
 *   糊帧、闭眼、空镜、主体被裁掉一半的混在一起（实测随手抽的 4 张里只有 1 张能用）。
 *   「比较 N 张图挑一张」是视觉模型能做、**出图模型完全做不了**的事：
 *   后者只会照着给它的那张图画，不会去比较。
 *
 * ★ 候选帧编号**从 0 开始**、顺序 = 请求里 `images` 的顺序。模板必须写明这一点：
 *   模型按「第几张」回答、调用方按下标取图，两边差一位就会**静默挑错帧**。
 *
 * ★ 视觉输入很吃 token：实测 tokenbox 上单张 1×1 的图就要 **4,396 prompt token**
 *   （中转站的固定开销，跟图大小关系不大）。所以候选帧的数量与分辨率都要克制，
 *   这也是本场景报价明显高于纯文本场景的原因。
 */
export const PUBLISH_COVER_PICK_PROMPT = `你是短视频封面选图师。下面按顺序提供了这条视频里的若干张候选画面，编号从 0 开始。

请挑出**最适合当封面底图**的那一张。

【评判标准 · 按重要性从高到低】
1. 画面清晰，没有明显运动模糊、糊帧或压缩噪点
2. 画面里有明确主体（人、菜品、或正在发生的动作），不是空镜、纯背景、一闪而过的过场
3. 主体完整入画：没有被裁掉半张脸或半盘菜，也没有贴在画面边缘
4. 状态有吸引力：有食欲感的菜品、有情绪或有动作的人
5. 构图相对平衡，上方或下方留有能压标题的空间

【候选画面的补充说明】
{{pickContext}}

【硬约束】
1. 只输出一个 JSON 对象，两个键：index（数字，你选中的画面编号）、reason（20 字以内的一句话理由）
2. index 必须是上面给出的编号之一；拿不准时选第一张
3. 不要 Markdown 代码块、不要解释、不要前后缀文字`

export const PUBLISH_COVER_PICK_FALLBACK = `{"index":0,"reason":"默认取第一张"}`

export const PUBLISH_SCENES = [
  {
    code: 'publish_material',
    name: '发布素材 · 标题/文案/封面画面描述（文本）',
    kind: 'TEXT',
    prompt: PUBLISH_MATERIAL_PROMPT,
    fallback: PUBLISH_MATERIAL_FALLBACK,
    temperature: 0.85,
    /**
     * 单次上限（文本场景是**安全网**，按 token 成本结算、恒不超过它）。
     *
     * 依据（2026-09-21 首次真实跑通的实测值）：
     *   输入 5510 token / 输出 341 token（输入大是因为与 copy_* 共用同一个 CONTEXT_BLOCK ——
     *   storyboard_generate 实测 5321，体量同族，不是这条场景特有）
     *   ⇒ 生产单价下（gpt-5.5: in 2880 / out 17280 分每百万）
     *     costFen = ceil(5510×2880/1e6) + ceil(341×17280/1e6) = 16 + 6 = **22 分**
     *   ⇒ 应扣 = 22 × 100 × 4 / 100 = **88 积分**
     *
     * ⚠ 所以**不能**用 100：那只剩 1.5 倍余量，而本项目的输出 token 波动极大
     *   （同一提示词连打 3 次成本能差 15 倍，变量是「思考 token」，见 setup-ai-channels.ts 文件头）。
     *   按既有规则「实测应扣 × 2 取整到 10」⇒ 88 × 2 = 176 ⇒ **180**。
     *   换个说法：输入固定吃掉 16 分，180 积分（= 45 分）留给输出 29 分 ≈ 1678 个输出 token，
     *   是实测 341 的 5 倍。
     * ⚠ 上限同时是**预冻结额**。180 会让「账户可用积分不足」的门槛抬高到 180
     *   （封面那条是 300，两步是先后发生的，峰值按 300 算而不是 480）。
     */
    beanPrice: 180,
    timeoutMs: 30000,
    maxRetries: 1,
    maxOutputTokens: 4000,
  },
  {
    code: 'publish_cover',
    name: '发布素材 · 封面图（图像，3:4 竖版）',
    kind: 'IMAGE',
    prompt: PUBLISH_COVER_PROMPT,
    fallback: PUBLISH_COVER_FALLBACK,
    temperature: 0.6,
    /**
     * ★★ 出图场景的 beanPrice 是初始预留额度，不是最终扣费。
     *    图像场景没有 token 用量，最终扣费读取 AiModel.unitPriceMicroFen 的实际单张成本。
     *
     * 定价依据（2026-09-21 实测，可原样复核）：
     *   ① 上游单价：tokenbox 计费表 `gpt-image-2` 的 `model_price = 0.10`（USD/张）。
     *      ★ 这个 0.10 是**真的量出来的**，不是抄价目表：
     *        `GET /dashboard/billing/usage` 的 `total_usage`（美分）在生成一张前后
     *        从 84 → 94，即 $0.10。注意出图 route 里 `resolution_tier` 报的是 `1k`，
     *        但计费跟的是**请求的模型名**而不是这一档（所以别指望按 1k 的价付钱）。
     *   ② 美元 → 人民币：0.10 × 7.2 = ¥0.72 = **72 分**
     *   ③ 分 → 积分：与文本同一把尺子
     *      `ceil(costFen × points_per_yuan × cost_multiplier / 100)` = 72 × 100 × 4 / 100 = **288**
     *   ④ 取 **300**：向上取整到 10，且不会因汇率小幅波动而低于成本。
     *
     * ⚠ 换模型 / 换尺寸都要重算（`TB_IMAGE_MODEL` / `AI_IMAGE_SIZE`）：
     *     gpt-image-2       @1024x1365  → 实测 $0.10 → 288 → **300**（当前）
     *     gpt-image-2-1k    @768x1024   → 价目 $0.06 → 172 → 180
     *       （★ 该变体**拒绝** 1024x1365：`exceeds channel resolution limit 1k`，
     *        而且它返回的是 b64 而不是 URL，比裸码慢一倍：实测 91s vs 45s。
     *        想省钱就把 TB_IMAGE_MODEL / AI_IMAGE_SIZE 一起换成这两个值，
     *        并把这里的 beanPrice 改成 180 —— 三者必须同时改，只改一个就会对不上账。）
     * ⚠ 汇率变了（TB_USD_TO_CNY）也要重算。固定报价不随成本浮动，
     *   上游涨价时差额由平台承担 —— 这是选「固定价」的代价，不是 bug。
     */
    beanPrice: 300,
    // 实测单张 22~45s（含排队），给 90s 留一倍余量
    timeoutMs: 90000,
    // 出图按张付费，重试 = 重复付钱；且失败多是上游硬故障，立刻重试不会好
    maxRetries: 0,
    // 图像路径不发 max_tokens（走 images/generations，不是 chat/completions），此值仅占位
    maxOutputTokens: 4000,
  },
  {
    code: 'publish_cover_pick',
    name: '发布素材 · 封面选帧（视觉：从候选真实画面里挑一张）',
    kind: 'TEXT',
    prompt: PUBLISH_COVER_PICK_PROMPT,
    fallback: PUBLISH_COVER_PICK_FALLBACK,
    // 选帧要的是判断力不是创造力：温度压低，避免同一批候选每次挑到不同张
    temperature: 0.2,
    /**
     * 单次上限（文本场景按 token 成本结算，这里既是安全网也是**预冻结额**）。
     *
     * ★ 实测数据（2026-09-24，tokenbox/gpt-5.5，两次真实调用）：
     *   · 4 张真实菜品帧 → in=6381 out=135，**5.6s**，costFen=21 ⇒ 实扣 **83 积分**
     *   · 6 张候选帧（本场景上限）→ in=9991 out=42，**19.5s**，costFen=30 ⇒ 实扣 **118 积分**
     *   ⇒ 单张图的固定开销 ≈ 1.3k prompt token（与图大小关系不大，是中转站侧的开销）。
     *   ★ 结论：它比「纯文本场景」贵一个量级，但远没有贵到最初估算的 548 积分。
     *
     * ★ 取值 **300**：实测最坏值 118 的 2.5 倍，留足 prompt 变长 / 帧数打满的余量，
     *   并与同组的 `publish_cover` 取齐（两者都是「一次几毛钱」的量级）。
     *   （2026-09-24 从 600 下调到 300：600 是没实测时的拍脑袋估算。）
     *
     * ⚠ `beanPrice` 只是**预冻结上限**，**不是用户实付金额** —— 结算走
     *   `settleAiCharge` 按实际成本算，超出预留会自动补充冻结差额（ai.service.ts 的
     *   `increaseReservation`）。所以给宽了不会多收用户的钱，只多占一点冻结额度。
     *   但**前端把它当报价展示**（「封面选帧最多 N 积分」），所以也不能随手写大。
     *
     * ⚠ 本场景失败**不致命**：兜底模板会选第 0 张，用户照样拿到封面（只是可能不是最好的那张）。
     *   所以这里保守一点比激进一点好 —— 但绝不能因为「反正有兜底」就不校准。
     */
    beanPrice: 300,
    /**
     * ★ 实测耗时**高度不稳定**：正常 5.6 ~ 19.5s，但 2026-09-24 有一次真实调用**超过 90s**
     *   （同一时段的图像生成也从 36s 涨到 85s ⇒ 那是中转站整体变慢，不是本场景本身的问题）。
     *   取 **120s**：约为实测正常值的 6 倍。
     *
     * ⚠ 不能再往上抬了。前端总超时 ≈ Σ(候选数 × 单场景超时 × (maxRetries+1))，
     *   见 `apps/mini/src/services/publish-material.ts` 的 PUBLISH_TIMEOUT_MS：
     *   当前 180(文本) + 120(本场景) + 90(封面) = 390s，刚好压在前端 420s 之内。
     *   再抬就会顶穿前端 —— 变成「前端还在等、nginx/网关先掐」。
     *
     * ★ 单候选、无原地重试：它就是「挑一张」，失败退回第 0 张比反复重试更划算。
     *   ⚠ 但要记得：本场景**超时会熔断整个 tokenbox-gpt 通道 300s**（通道级熔断，
     *   见 circuit-breaker.ts），连带把同一通道上的 `publish_material` / `publish_cover`
     *   一起冷藏 —— 这正是超时必须克制、不能随手给 300s 的原因。
     */
    timeoutMs: 120_000,
    maxRetries: 0,
    // 输出只有 `{"index":N,"reason":"…"}` 十几到几十个 token；给宽是防止 JSON 被截断成
    // 半截导致解析失败（解析失败会退第 0 帧，静默降级）。与 SCENE_OVERRIDES 保持一致。
    maxOutputTokens: 2000,
  },
] as const

// ─────────────────────────────────────────────────────────────────────────────
// AI 剪辑决策（EDL）—— 2026-09-22 新增
//
// ★ 它是本项目**第一个「模型直接驱动剪辑工具」的场景**：此前 AI 只写文案与分镜，
//   「怎么剪」完全由用户在面板上选的固定档位决定（转场 3 档 × 节奏 3 档 × …），
//   出片永远像模板套的 —— 用户的原话是「没有转场和剪辑」。
//   本场景让模型拿到**逐镜头的台词与素材时长**，直接给出剪辑参数。
//
// ★ 输出被 `src/render/edl.ts::parseEdl` 解析，**只认 JSON**：
//   解析失败一律退回用户档位（绝不阻塞出片）。所以这里的 JSON 结构必须与它一致 ——
//   改字段名要两边一起改，否则会静默退化成「永远按档位剪」而**不报任何错**。
// ─────────────────────────────────────────────────────────────────────────────

export const EDIT_PLAN_PROMPT = `你是资深短视频剪辑师。下面这条片子的素材已经拍好、配音稿也写好了，
你的任务不是写文案、也不是改台词，而是**决定这条片子该怎么剪**。

【逐镜头素材与台词】
{{shotPlanInput}}

【用户在面板上选的倾向】{{preferPlan}}
【用户备注】{{note}}

输出要求：
1. shots：为**每一个镜头**给出目标时长 targetMs（毫秒，整数），index 从 0 开始，必须与上面的镜头编号一一对应、**一个都不能少**。
   · 判断依据是这句台词的信息量与长度：交代信息的长句给 3000~4500；短促有力的句子（如「就是这一口」）给 1200~2000。
   · **绝不能超过该镜头的素材时长**，并请留出约 10% 余量（转场要占用两侧素材）。
   · 时长要有**起伏**：所有镜头给同一个数字等于没剪；相邻镜头尤其不要雷同。
2. pacing（整体节奏）：NATURAL=自然叙述｜FAST=快切，适合探店/种草｜STORY=舒缓，适合品牌故事
3. transitions（转场）：CLEAN=硬切，最利落｜SMOOTH=柔和叠化，适合温馨叙事｜DYNAMIC=甩镜动感，适合强节奏
4. subtitleStyle（字幕样式）：CLEAN=基础白字｜EMPHASIS=黄字黑边，促销强调｜SOCIAL=社媒卡片风
5. bgm（配乐）：NONE=不要｜LIGHT=温馨轻音乐｜UPBEAT=轻快电子｜PREMIUM=高级弦乐钢琴
6. reason：每个镜头一句话说明取舍理由，不超过 20 字
7. **只输出 JSON 对象**，不要 Markdown 代码块、不要任何解释：
{"pacing":"","transitions":"","subtitleStyle":"","bgm":"","shots":[{"index":0,"targetMs":3000,"reason":""}]}`

/**
 * 兜底模板。
 *
 * ★ `shots` 故意留空 —— 网关走兜底时 `edit-plan.service.ts` 会先看 `isFallbackTemplate`
 *   直接退回档位，根本不会拿它去解析；但万一有人改了那条判断，空 shots 也会让
 *   `parseEdl` 返回 null（它的判据就是「一个可用镜头时长都没解析出来」）
 *   ⇒ 仍然退回档位。**两道保险都不改出片结果**，这是有意的冗余。
 */
export const EDIT_PLAN_FALLBACK = `{"pacing":"NATURAL","transitions":"SMOOTH","subtitleStyle":"CLEAN","bgm":"NONE","shots":[]}`

export const EDIT_PLAN_SCENE = {
  code: 'edit_plan',
  name: 'AI 剪辑决策 · 逐镜头时长与整体节奏（文本）',
  kind: 'TEXT',
  prompt: EDIT_PLAN_PROMPT,
  fallback: EDIT_PLAN_FALLBACK,
  temperature: 0.5,
  /**
   * 单次上限（文本场景是**安全网**：按 token 成本结算，恒不超过它，超出由平台承担）。
   *
   * ⚠ **这个数字是估算，尚未实测**（与 publish_material 那种「算完再定」的不同）：
   *   本场景的输入只有逐镜头清单与倾向说明（预计 600~900 token，远小于
   *   publish_material 的 5510），输出是一份 JSON（6 镜头约 300~400 token）。
   *   按生产单价（gpt-5.5: in 2880 / out 17280 分每百万）估：
   *     输入 ~800 token → ceil(800×2880/1e6) = 3 分
   *     输出 ~400 token → ceil(400×17280/1e6) = 7 分
   *     合计 10 分 ⇒ 应扣 = 10 × 100 × 4 / 100 = **40 积分**
   *   沿用既有规则「实测应扣 × 2 取整到 10」⇒ 80；考虑到**思考 token 的波动可达 15 倍**
   *   （见 setup-ai-channels.ts 文件头），取 150 留出约 3.7 倍余量。
   *   ★ 真跑一次后应回来按实测值校准，并把上面的「估算」改成实测口径。
   * ★ 输出预算抬到 4000 之后，理论最坏（思考与正文吃满）约 73 分 ⇒ 292 积分；
   *   超出本上限的部分由平台承担 —— 这正是「安全网」的语义，与其它文本场景一致。
   */
  beanPrice: 150,
  /**
   * ★★ `timeoutMs` / `maxRetries` / `maxOutputTokens` 与
   *   `scripts/setup-ai-channels.ts` 的 `SCENE_OVERRIDES.edit_plan` **必须同值**。
   *
   *   分工：`ai-prompts:sync` 只在**建行时**写这三个字段，update 分支一概不碰；
   *   `ai-channels:setup` 用 SCENE_OVERRIDES 覆盖它们。两处不一致的后果是
   *   「手工重建一次场景，配置就悄悄退回另一套」—— 没有任何地方会报错。
   *
   * ★ 90s / 0 重试是**实测口径**（2026-09-22 上线首跑，任务 15）：gpt-5.5 单次 46.6s
   *   （prompt 5097 token），60s 预算只剩 29% 余量 ⇒ 镜头一多必然超时。
   *   完整依据与「为什么还要补一个 DeepSeek 备用」写在 SCENE_OVERRIDES 那一段。
   */
  timeoutMs: 90_000,
  maxRetries: 0,
  /**
   * ★★ 必须 ≥ `SCENE_MIN_OUTPUT_TOKENS`（默认 4000），**不是**「JSON 只要 400 token 就给 1200」。
   *
   * 理由与 `setup-ai-channels.ts` 文件头第 4 条同一口径：推理模型把 `max_tokens` 当作
   * **思考 + 正文共用**的预算。1200 会被思考吃光 ⇒ 返回 `finish_reason='length'` 且
   * `content=''`，而 HTTP 200、报文结构完全合法。网关把空正文判成 `BAD_RESPONSE`，
   * 那是**非通道级**故障 ⇒ 它会在**同一个通道上**按 `maxRetries` 反复重试（这里 1 ⇒ 白等 2 次），
   * 最后抛错被 `generateEditPlan` 接住 ⇒ 表现是「**每一次 AI 档出片都静悄悄按面板档位剪**」，
   * 不留任何报错。（2026-09-22 复核时改的：原值 1200 只算了「输出 400 × 3 倍」，漏了思考预算。）
   *
   * ★ 它是**安全网**而不是目标值：结算按实际 token 成本走，正常输出仍只有 300~400 token。
   * ★ 本场景**不在** `LOW_REASONING_SCENES` 里（v1 不压思考预算）。要往里加，必须先按
   *   `scene-codes.ts` 那张表的口径在生产真打一轮、拿到「成功 / 耗时 / 正文长度」再定 ——
   *   ⚠ 尤其 `claude-sonnet-5` 完全无视 `reasoning_effort`，不能进这类场景的候选链。
   */
  maxOutputTokens: 4000,
}
