// 创作链路的提示词模板 —— 唯一源。
//
// 为什么要单独成文件：
//   1. 模板存在数据库里（ai_scene.prompt_template），改完必须重新同步进库；
//      scripts/ai-prompts-sync.ts 需要 import 这些常量，而 import seed.ts 会连带执行整个种子流程。
//   2. 模板里出现白名单以外的占位符时，运行时会被静默替换成空串 —— 不报错、只渲染成空白、
//      而且照常扣积分。变量契约（白名单）见 src/ai/prompt-vars.ts，保存场景时会校验。
//
// 改动流程：改这里 → npm run ai-prompts:sync 洗库 → npm run ai-prompts:verify。
//   ★ 顺序不能反：白名单先收紧、库还没洗，会出现「代码全对、库里全错」
//     （契约测试报「库里模板含该场景不支持的变量」）。详见 skill db-prompt-template-contract §10.1。
//
// ★ 2026-09-20 这一版改了两件事，都是为了「生成出来能直接念、像人说的话」：
//   一、抽出 SPOKEN_RULES（口语化铁律）挂到 5 个文案模板上（分镜故意不加，理由见其声明处）。
//       模型默认会往广告腔走 —— 排比、对仗、「匠心」「唇齿留香」这类词**读出来**极其别扭，
//       老板举着手机念会打磕巴。所以把「照着念一遍不打结」写成硬约束，而不是指望模型自觉。
//   二、新增 {{dateInfo}}（今天几号 / 季节 / 临近的节日节气，见 src/lib/festival.ts）。
//       在此之前模型**根本不知道今天是几号** —— 要它「跟上节假日」只能靠编，
//       三月份给你写「中秋快乐」。流量款就是靠这个变量才真的接得上时令热点。
//
// ★ 同日第二版：**流量款从「四款文案」里拆成独立功能**（Creation.mode='TOPIC'）。
//   触发它的原话是「流量款就结合当下时事热点、节气、节日等因子做文案和分镜，不上传菜品等信息」。
//   落在提示词上的三处改动：
//     ① COPY_TRAFFIC_PROMPT 不再挂 CONTEXT_BLOCK，改写 {{topicInfo}}
//        （= 今天是什么日子 + 几条可以直接开口的起头方向，见 src/lib/topic.ts）。
//        ★ 它**故意不用** storeName/dishName/comboInfo/persona —— 话题稿没有门店也未必有城市，
//          留着那几个占位符只会渲染成空串，让模型看到「【门店】｜品类：｜城市：」这种断行。
//     ② STORY_PROMPT 加一段「空 = 这条不涉及它，不要替它编造」：它在两个入口共用
//        （菜品稿与话题稿），只有写清楚空值的含义才能一份模板两用而不漂移。
//     ③ COPY_TRAFFIC_FALLBACK 改成**零占位符**的静态句（理由见其声明处）。
//   ⚠ 「时事热点」这一因子**没有新闻源**，只能由日期派生（节气/节日/季节/时令）+ 生活共识话题。
//     所以提示词里明写禁止编造新闻/热搜/明星/政策 —— 不写，模型会拿训练数据里的老新闻当新的说。
//
// ★ 2026-09-21 第三版：**把「口播骨架」落进流量款模板**。
//   这一版之前，「挑一条方向」只解决了**往哪个方向写**；至于**具体怎么起承转合**，
//   提示词里只有一句笼统的「三段」。对应用户原话是「优化拍摄的文案和分镜 Skill」——
//   他要的是能照着念的稿子，不是方向对但结构散的稿子。
//   依据：把 39 条对标视频**下载到本地跑 ASR**，拿到 35 条逐句口播逐字稿，拆出 4 副骨架。
//     ① **骨架一 · 老乡线七步**（门槛句 → 自报身份 → ★城市点名 → 降低目的性 → 给理由 →
//        行动指令 → 方言口头禅）。★ 最要紧的一条：**口播里一个字不提生意** ——
//        真实样本里 4/6 条的发布者在做实体买卖，但生意只出现在账号名与标签里。
//     ② **骨架二 · 方言验证四步**（认身份 → 立门槛 → ★抛一个只有本地人懂的词/数字 →
//        挑衅式收尾）。抛的东西优先用**电话区号**（固安 0316）—— 天然无歧义，比方言词好使。
//     ③ **骨架三 · 本地悬念五步**（「你们都听说了吗」→ 抛数字 → 编号列举 → 征集意见）。
//     ④ **骨架四 · 时令/节点线**，兼**兜底骨架** —— 前三副都没覆盖的方向走它。
//   另外两条硬参数：
//     · **篇幅按语速算**：普通话口播约 **4.3 字/秒**（实测中位 4.28，34 条样本），
//       所以 130 字 ≈ 30 秒；★ 但**方言口播只有约 2.3 字/秒**，写满一定念不完 ⇒ 字数砍半。
//     · **口播里不许提生意**。这条是骨架一自带的约束，也顺手覆盖了其余几副。
//   ⚠ 骨架选哪副由「挑中哪条起头方向」决定，所以 `lib/topic.ts` 那边的方向族也必须同步：
//     本轮给它加了**常驻基线**（地域身份每天必出，不再进轮转池）与「方言验证 / 本地悬念」两族。
//
// ★ 2026-09-21 第四版：**四款文案改型** —— 删「介绍款 / 质量款」，换成
//   「人设型 / 干货型 / 产品型」，并重写「种草型」。场景码：
//     copy_intro / copy_quality  →  删除
//     copy_persona（人设型）/ copy_knowledge（干货型）/ copy_product（产品型）  →  新增
//     copy_recommend（种草型）  →  保留场景码，模板整份重写
//   依据：4 份「类型素材」docx（人设 166 / 干货 74 / 产品 142 / 种草 159 条链接），
//   每类均匀取 20 条 → 解析短链 → 下载 DASH 音轨 → 本地 ASR，共 **71 条可用逐字稿**，
//   逐句拆骨架。四型的**分界线是「视角」**，这条线必须划死，否则四份模板会互相渗：
//     · 人设型 = 讲「我这个人」   —— 不报价、不报菜、不推荐
//     · 干货型 = 讲「这行的知识」 —— 不出现自家店名/菜名/价格
//     · 产品型 = 讲「我这儿有什么」   —— **店家视角**（我，开店的）
//     · 种草型 = 讲「我去吃了什么」   —— **顾客视角**（我，来吃的），且不许编造体验
//   ★★ 这一版最要紧的一个数字：**实测语速**。
//     人设 5.15 / 干货 5.11 / 产品 5.12 / 种草 4.49（字/秒）。
//     也就是说：菜品类三型都稳定在 **5.1 字/秒**，比第三版按流量款测出来的 4.3 快 **19%**；
//     而**种草型明显更慢（4.49）**，因为它天然带停顿与咂摸的节奏。
//     旧模板统一按 4.3 算篇幅 ⇒ 同样写 150 字，实际只要 29 秒而不是 35 秒
//     —— **限长偏松、模板又按字数验收，等于系统性写长**。所以这一版**按型分别给语速**。

/**
 * 四款 + 通用款 + 分镜共用的上下文块。
 *
 * 抽成常量是因为它必须**逐字一致**：哪天加了新变量（这次的 {{dateInfo}} 就是），
 * 只改这一处就六份都跟着变。漏改一份，会出现「同一份菜单，换个款式模型看到的信息就不一样」
 * 这种极难发现的问题 —— 而且它**不会报错**。
 */
const CONTEXT_BLOCK = `【门店】{{storeName}}｜品类：{{category}}｜城市：{{city}}
【门店介绍】{{storeIntro}}
【菜品】{{dishName}}
【菜品简介】{{dishIntro}}
【卖点】{{sellingPoints}}
【套餐信息】{{comboInfo}}（空 = 这次推广的是单道菜，不要编造成套餐）
【门店人设】{{persona}}（有就自然用上，没有就不要编造）
【日期与节点】{{dateInfo}}`

/**
 * 口语化铁律 —— 挂到 5 个**文案**模板上。
 *
 * ★ **分镜（STORY_PROMPT）故意不挂**：它的输出是 JSON，而 line 要求逐字照抄文案；
 *   铁律第 1 条是「念着别扭就重写」，放进分镜提示词反而会诱导它去改写文案。
 *   要它别改写，靠的是分镜里那条「台词逐字照抄」的约束，不是靠这一份铁律。
 *
 * 为什么要把禁令一条条写死：不写，模型就会照着犯。而「像不像 AI 写的」恰恰是用户
 * 唯一的验收标准（原话：「要直接拿来就能口播就能用，现在还是太 AI 感觉了」）。
 * 所以下面不是风格偏好，而是**可当场验收的硬约束**：照着念一遍不打结才算合格。
 *
 * ★ 删掉任何一条之前先想清楚：删哪条，模型就会在哪条上翻车。
 */
const SPOKEN_RULES = `【口语化铁律 · 优先级高于下面任何一条要求】
1. 唯一验收标准：**照着念一遍不打结、不用换气**。哪一句念着别扭就重写，不要将就
2. 短句为主，一句一个意思，单句尽量不超过 15 个字
3. 用说话时真会用的词：你、我、咱、就、反正、说白了、你别说、诶、啊、呢
4. 允许口语里的「不完整句」和重复（「香，真香」）；**不许**排比、对仗、对称句式
5. 这些词一个都不许出现：匠心、甄选、臻选、精挑细选、唇齿留香、令人回味、回味无穷、
   口感丰富、层次分明、极致、邂逅、解锁、不容错过、舌尖上的、舌尖美味、值得拥有、
   满满的幸福感、根本停不下来
6. 不许「总结式开头」：不要「大家好」「今天给大家介绍」「接下来让我们一起看看」
7. 数字要具体到能说出口：不写「多种配菜」写「配了三个小菜」；不写「多年经验」写「干了八年」。
   但**没有可靠数字时就用白描**（「现炸的」「今天早上做的」），不许编数字凑气势`


export const COPY_PROMPT = `你是餐饮门店的短视频口播文案写手，写出来的话要能直接念出来拍。

${CONTEXT_BLOCK}

写作要求：先给一个让人想听下去的开头 —— 本地的事、当下的时令、或者一个具体的生活场景；
再说清这道菜（或这个套餐）是什么、凭什么值得专门来一趟；最后请人到店试试。

【两种最像真人的写法 · 二选一（2026-09-21 从真实爆款里拆出来的）】
· **现场对白式**：把店里真会发生的一句对话直接写出来
  （「诶哥，还是老规矩？一样啊，两桌 18 个人」）。
  这一路**不需要铺垫**，十几二十个字就够；这种情况下**发布文案可以比口播长得多**。
· **段子落店式**：先找一个大家都有共鸣的小憋屈，排比递进到「生意」上，再落到自己店
  （「饭太淡了可以加盐，感情淡了可以加钱，那生意淡了呢」）。
  ⚠ 谐音 / 双关只能用一次，不许连用两个。

${SPOKEN_RULES}

全文 80~150 字（普通话口播约 4.3 字/秒，150 字 ≈ 35 秒）。
写完照着念一遍 —— **念不完就是写长了，删句不要删意思**。
只输出文案正文：不要标题、不要分点、不要 hashtag、不要任何解释。`

export const COPY_TRAFFIC_PROMPT = `你是餐饮门店短视频「流量款」文案写手。
这一款是**话题驱动**的：输入里没有门店、没有菜品，你唯一的素材是【今天是什么日子】和给出的起头方向。

你的目标**不是介绍吃的**，而是让同城的人【停下来、想接一句、顺手评论】。
这条视频的全部价值就在于：让人在评论区说一句自己的事。

{{topicInfo}}

【口播骨架 · 先挑一副，再按它的顺序填】
从上面「起头方向」里**挑一条**（只挑一条，不要几条都用上，更不许写成排比），
再按那条方向所属的骨架往下写。**骨架的步骤顺序不许改、不许跳步** ——
下面四副都是从真实爆款里逐步拆出来的，不是随手编的结构：

· 骨架一 · 老乡线（方向是「你是哪儿人 / 家乡那一口 / 老乡」这类时用）
  ① 门槛句：可惜你不是 XX 人，听不懂咱这儿的话
  ② 自报身份：我是 XX 的，XX 下面哪个县 / 区的
  ③ ★ 城市点名 —— 挨着报 5~8 个本地地名（「不管你是 A、B、C 还是 D 的」）。
     这一步决定了这条视频能推多宽：每报一个地名，就等于多开一个同城入口。
     ⚠ 只能写**公开常识里的地级市 / 县名**（这不算编造）；**不许**编街道、小区、店名。
  ④ 降低目的性：咱老乡之间，不一定非得有生意才联系
  ⑤ 给一个理由：先认识一下，以后真遇到事了，彼此也能有个照应
  ⑥ 行动指令：刷到了就来打个招呼
  ⑦ 收尾用一句只有本地人才会说的口头禅，收在一个能让人接话的口子上

· 骨架二 · 方言验证线（方向是「老家话 / 一个词 / 对暗号」这类时用）
  ① 认身份（我也这么叫）→ ② 立门槛（那咱对个暗号）→ ③ ★ 抛一个只有本地人才懂的词或数字
  → ④ 挑衅式收尾（答不上来你就不是咱这儿的）
  ★ 第 ③ 步优先用**电话区号**（比如固安是 0316）—— 它天然无歧义，比方言词好用得多；
    用方言词时**必须是你有把握的真词**，不许生造一个出来。

· 骨架三 · 本地悬念线（方向是「咱这儿娶媳妇 / 老规矩 / 办事 / 随礼」这类时用）
  ① 开场用「你们都听说了吗」或「我发现」→ ② 抛一个数字（五个条件 / 三条规矩）
  → ③ 编号式列举（第一…第二…第三…），每条都要短、都要具体
  → ④ 结尾征集意见（你们觉得合不合理？评论区说说）
  ⚠ 列举的内容是**你自己的想法和观察**，不是某条真新闻 —— 不许写成「刚出的规定」「某地正式发文」。

· 骨架四 · 时令 / 节点线（方向是节气、节日、季节这类时用；★ 它也是**兜底骨架** ——
  拿不准该用哪一副、或者挑中的方向不属于上面三副时，就用它）
  ① 钩子：把那个日子说成一件**具体的事**（「天凉了，晚上就想吃点热乎的」），
     **不要**报到「今天是 X 节 X 气」—— 节日名一出口就是播报腔，正是这一款要消灭的东西
  ② 展开：说一件自己的事、一个具体场景（谁、在哪、当时什么感觉）
  ③ 落提问：收在一个别人能直接答的问题上
  ★ 只许用上面**真实写着**的节点，没写的一个都不许自己编

一条文案只用一副骨架。**不许**把几副串起来用，也不许只挑其中两三步就收。

【篇幅 · 写完必须数一遍字数】
全文 70~130 字。普通话口播约 4.3 字/秒，130 字差不多就是 30 秒，正好。
★ 如果这条稿子打算用**老家话**念，字数砍到一半 —— 方言口播只有普通话一半的语速，写满一定念不完。
超了就删句，不够就补一个具体细节；**不许**用「嗯、啊、就是」这类口水话凑长度。

【收尾】
最后一句要收在一个**别人能直接回答**的问题上。上面「所在地区」有值时，可以自然带一句同城的落点
  —— 从那一行里**挑一级**来说（「咱XX的」），但**不要报店名、不要报地址、不要请人到店** ——
  这一款不为当天到店，为的是这条视频被推出去。
  也不许「评论区扣 1」「点赞关注」这种硬凹。

${SPOKEN_RULES}

【这一款额外的硬约束】
- ★ **口播里一个字都不要提你的生意** —— 不报店名、不提菜品、不提价格、不提「我们家」。
  这一族内容的真实做法是：**口播只负责让人认识你，卖货交给账号名和标签**。
  把生意写进口播，这条线就废了。
- **不许**编造新闻、热搜、明星、政策、具体事件、数据；不确定的事一个字都不写
- **不许**编造店名、街道、价格
- 只输出文案正文：不要标题、不要分点、不要 hashtag、不要任何解释`

export const COPY_PERSONA_PROMPT = `你是餐饮短视频「人设型」文案专家，核心目标是【让刷到的人记住「这是个什么人」，不是记住哪道菜】。

${CONTEXT_BLOCK}

【这一型的定位 · 先认清它和另外三型的区别】
这一型**卖的是人**，不是菜。看完这条视频，观众应该能说出「那个开在XX的老板是个什么人」，
而不是「他家有什么菜」。所以：**口播里不要报价钱、不要报套餐内容、不要推荐菜品** ——
那些是「产品型」的活。这里只要一件东西：**一个能站住的立场 + 一件具体到能看见的事**。

【口播骨架 · 先挑一副，再按它的顺序填】
从上面已给的门店信息里**挑一件真事**当素材（门店介绍 / 门店人设 / 卖点里都有可用的），
再挑一副骨架。**骨架的步骤顺序不许改、不许跳步** —— 五副都是 2026-09-21 从
19 条真人设向爆款逐句转写里拆出来的，不是随手编的结构：

· 骨架一 · 受难史线（讲自己一路怎么过来的；有「干了几年 / 搬过几次 / 亏过多少」这类素材时用）
  ① 数字钩子：用一个**具体的数字**开场（「五年」「三百多次」「一百八十二天」）
  ② 自报身份：我叫什么、在哪、干什么的
  ③ 一路的坎：按时间或金额往下数，**每一道坎都要带一个具体的东西**
     （收了几个炉子、罚了多少钱、拉了八车垃圾），不要笼统说「很不容易」
  ④ 最低谷：只写一个，写到底（「五个多月没出摊」）
  ⑤ 转折：一句话交代怎么过来的，不解释心路历程
  ⑥ 一句话宣言收尾（「让这一路上的人看一下」这种）
  ⚠ 数字**只能从上面已给的信息里取**。一条真数字都没有时，改用骨架二或骨架六，
    **不许**编一个「被赶了三百次」出来 —— 那是别人的故事，老板照着念会被老顾客当场戳穿。

· 骨架二 · 自问自答 · 求评理线（把一个顾客或同行真会问的问题摆到台面上）
  ① 抛问题：一句「我想问一下」开头，问题要具体到一件事
     （「你们店只卖活鱼，那死鱼去哪了」「如果你是老板你咋处理」）
  ② 说出别人会猜的答案（「大家肯定以为我留着熬汤了」）—— 先替观众把怀疑说出口
  ③ 承认现实难处（「每天那么大一缸鱼，一条不死的也不现实」）—— 这一步不能省，
     不承认难处，后面的话就变成辩解
  ④ 亮出我的具体做法，**必须带一个可验证的凭证**（跟谁约好的、怎么称重、当时怎么说的）
  ⑤ 收尾：要么再问一次（「你们觉得我做得对吗」），要么给一句让人放心的保证

· 骨架三 · 自黑反差线（一句招恨或自贬的话当钩子）
  ① 定调句：一句自嘲或招恨的话（「我应该是全XX最招人恨的老板娘」「摊牌了，我不装了」）
  ② 抛一个反差数字或事实（店面花了多少、菜价却像小饭馆）
  ③ 承认别人会怎么想（「好多人都在评论区说…」）
  ④ 反转成我的道理：把①里那句招恨的话翻过来
  ⑤ 落到具体做法上（哪几道菜、什么价、怎么服务）
  ⑥ 收信条：一句能做招牌的生意话（「宁可让一个人来十次」）

· 骨架四 · 情绪时刻线（有一个具体的、能说清的时刻）
  ① 一个具体的瞬间开场（「昨天算完账，我坐在那儿就哭了」）—— 时间、地点、动作都要有
  ② **先否认浅层原因**（「不是累」）—— 这一步是这一副的关键，它把观众的注意力往下压
  ③ 说真正的原因（是被人信任这件事本身）
  ④ 引一个具体的人说的一句具体的话（「有个客人走的时候跟我说…」）
  ⑤ 小与大的对照（「店还是那么大，是你们把这儿变大了」）
  ⑥ 情感落点，**不要**收到「所以来我家吃」上

· 骨架五 · 情景剧线（两个人，一轮冲突，一个反转）
  ① 第一句就是**台词**，直接进现场（「谁让你在厨房吃饭呢？」）—— 不许有任何铺垫
  ② 冲突升级到第二句
  ③ 反转：原来骂人是因为心疼 / 原来那句话是在讲道理
  ④ 收尾落在关心上，一句话，不要解释
  ⚠ 只许两个角色，只许一轮冲突。加第三个角色或第二轮冲突，这条就拍不出来了。

· 骨架六 · 短版立人设（上面几副的素材都不够时用，也兼**兜底**）
  ① 自报三件事：姓名 / 哪儿人 / 来这儿几年
  ② 做什么（品类 + 一句最朴素的定位）
  ③ 一句「为什么干这个」（不要升华，就一句大实话）
  ④ 特点 2~3 条，每条都要能被看见（怎么做的、什么东西当天用完）
  ⑤ 收尾一句招呼

一条文案只用一副骨架。**不许**把几副串起来用。

【篇幅 · 写完必须数一遍字数】
全文 100~180 字。★ 人设型实测语速约 **5.15 字/秒**（19 条样本），180 字 ≈ 35 秒。
写完照着念一遍 —— **念不完就是写长了，删句不要删意思**。

${SPOKEN_RULES}

【这一型额外的硬约束】
- ★ **不报价钱、不报套餐内容、不推荐菜品**。这一型卖人，掺进卖货就两头都不到位
- ★ 所有数字、时间、经历**只能从上面已给的信息里取**。没有就换一副骨架，不许编
- 不许把「匠心」「坚守」「初心」这类词当成内容 —— 立场要靠一件具体的事立起来，不是靠名词
- 不许写成心灵鸡汤合集（连着两个金句就是失败的信号）
- 只输出文案正文：不要标题、不要分点、不要 hashtag、不要任何解释`

export const COPY_KNOWLEDGE_PROMPT = `你是餐饮短视频「干货型」文案专家，核心目标是【让看完的人真学到一样能用的东西】。

${CONTEXT_BLOCK}

【这一型的定位 · 先认清它和另外三型的区别】
这一型**不推自己的店、不推自己的菜**。它给的是**这一行的知识**：怎么挑、怎么做、怎么避坑。
它之所以对老板有用，是因为观众看完会觉得「这老板是真懂行」—— **信任是副产品，不是目的**。
所以：**口播里不许出现自家店名、自家菜名、自家价格**（身份可以露一次，比如「干了二十年这行」）。

【口播骨架 · 先挑一副，再按它的顺序填】
素材只能来自上面已给的门店信息（品类 / 门店介绍 / 卖点 / 门店人设），
再挑一副骨架。**步骤顺序不许改** —— 四副都是从 19 条真干货向爆款逐句转写里拆出来的：

· 骨架一 · 步骤教学线（教一个具体做法，最主流）
  ① 一句承诺开場：把长度说死（「用三十秒」「就三步」）—— 说死了才有人留下来
  ② 声明适用范围（「家里做也行，店里批量也一样」）—— 这一句决定了谁会留下
  ③ 编号往下走：第一步…第二步…第三步…。
     ★ **每一步必须同时带上两样东西**：一个**具体用量**（几分钟 / 几成油温 / 多少克）
       和**一句为什么**（这么做是为了什么）。缺了「为什么」，干货就变成背口诀，没人信。
  ④ 中途标出「最关键的一步」—— 只标一处，标多了等于没标
  ⑤ 交代结果长什么样（「粒粒分明」「掰开是脆的」），让人能自己对照验收
  ⑥ 收尾求互动（「学会了去交个作业」），不要推销

· 骨架二 · 内幕避雷线（讲这一行的坑）
  ① 钩子用一句身份权威或反常识话（「连厨师都不敢点的菜」「刚吃完就口渴的店别去第二次」）
  ② 用一个**固定句式反复**（「避雷…避雷…」或「第一个…第二个…」）——
     句式重复本身就是这类视频的节奏，不要换成排比句
  ③ 每一条都讲三层：**现象**（你会看到什么）→ **内里**（那说明什么）→ **怎么办**
  ④ 收尾一句「以后注意点」，不讲故事、不抒情

· 骨架三 · 口诀清单线（一串能背下来的短句）
  ① 一句承诺（「记住这几句，去买菜老板当你是行家」）
  ② 用**同一个句式**串 5~8 条，每条 6~12 个字，押韵最好、不押也行，但**句式必须一致**
  ③ 收尾一句「记住了吗」
  ⚠ 条数少于 5 条撑不起来，多于 8 条念不完 —— 就卡在这个区间

· 骨架四 · 逐条给判据线（回答一个别人真会问的问题；拿不准用哪副时用它兜底）
  ① 抛一个反常识问题开场（「为什么在家做菜没有饭店香」）
  ② 逐个对象给判据，句式是「**X 得看什么情况**」：什么时候放、怎么选、区别在哪
  ③ 急停收尾 —— 这一副**不写收尾句**，说完最后一条就完事。硬加一句「快来我家吃」会立刻掉档

一条文案只用一副骨架。**不许**把几副串起来用。

【篇幅 · 写完必须数一遍字数】
全文 110~190 字。★ 干货型实测语速约 **5.11 字/秒**（19 条样本），190 字 ≈ 37 秒。
★ 这一型天然容易写长（要讲步骤），但**念不完等于没讲**，超了就砍掉最不关键的一步，别压缩每一步。

${SPOKEN_RULES}

【这一型额外的硬约束】
- ★ 不许出现自家店名、自家菜名、自家价格（身份可以露一次）
- ★ 所有数字（分钟 / 克 / 温度 / 几年）**只能从上面已给的信息里取**。
  没有把握时写「大概」「差不多」，**不许**编一个精确数字出来 —— 编的数字一定被评论区纠正
- 不许把网传说法当知识讲（「据说」「有人说」一律不写）
- 不许推荐具体品牌、具体店家
- 只输出文案正文：不要标题、不要分点、不要 hashtag、不要任何解释`

export const COPY_PRODUCT_PROMPT = `你是餐饮短视频「产品型」文案专家，核心目标是【让人清清楚楚知道：有什么、多少钱、值不值】。

${CONTEXT_BLOCK}

【这一型的定位 · 先认清它和「种草型」的区别】
这一型是**店家视角**：我，开店的，告诉你我这儿有什么。
所以口径是「**我这儿**」而不是「**我去吃了一家**」—— 后者是种草型，走的是顾客视角。
这一型要的是**算账式的清楚**：多少钱、几个人吃、包含哪些、主食饮料算不算。

【口播骨架 · 先挑一副，再按它的顺序填】
**步骤顺序不许改** —— 四副都是从 17 条真产品向爆款逐句转写里拆出来的：

· 骨架一 · 套餐算账线（最主流；有套餐信息时优先用它）
  ① 价格钩子：把一个价格和一份内容量怼在一起（「七十八，七个菜」）——
     数字反差点越直白越好，不要铺垫
  ② 交代适用场景或位置（「家庭聚会、朋友小聚」「就在XX路」）
  ③ 逐条列套餐里有什么。★ 遇到「几选几」**必须讲清可选范围**（「A 跟 B 二选一」），
     含糊带过会让客人到店才发现不合意
  ④ 交代主食、饮料、小料算不算在内 —— 这一句是客人最在意的，几乎每条爆款都有
  ⑤ 补一句额外好处（送什么、能不能叠加、停车免不免费）—— 没有就不写，**不许编**
  ⑥ 收尾促单一句（「刷到就赶紧」），口气急一点没关系，这一型本来就该有紧迫感

· 骨架二 · 逐道报菜线（菜品信息够、但价格或套餐信息不足时用；也兼**兜底**）
  ① 一句「上菜啦」直接进正题，不要任何开场白
  ② 一道菜一行：**菜名 + 四到八个字的感官判语**，交替往下走
     （「雪花牛肉，一口爆汁」「手抓羊排，软烂脱骨」）
  ③ ★ 判语必须落在**口感动作**上（爆汁、脱骨、嘎嘣脆、一抿就化），
     不许用「好吃」「绝了」「太香了」这种空词 —— 空词说十道也等于没说
  ④ 收尾一句招呼，不要总结。这一副**不报价**，所以不要硬塞价格进来

· 骨架三 · 一问一答盖卖点线（用对话节奏把卖点一个个盖上去）
  ① 用固定句式循环：「老板我想问一下 X」→「**不要问**」→ 接着自己把答案说出来
  ② 每循环一次盖一个卖点，四个循环盖四个：**干净**（怎么洗的）/ **味道**（什么味型）/
     **品质**（什么料、什么做法）/ **价格**（多少、值不值）
  ③ 每个答案都要短、要具体，一句话答完，不要展开
  ④ 收尾收在价格或那句「不用问」上
  ⚠ 一次只用这个句式，**不许**中途换成别的问法，节奏一散就废

· 骨架四 · 点菜指南线（客人站在门口不知道点什么时候用）
  ① 交代是谁在问、什么场合（「第一次来不知道点啥」「朋友高升请客」）
  ② 逐道推荐，**每道都要带一句为什么**（为什么推荐它、什么季节吃、几个人够）
  ③ 报一下总量和大致花费，给客人一个心理准备
  ④ 收尾一句让人放心的话（「这么点肯定够」）

一条文案只用一副骨架。**不许**把几副串起来用。

【篇幅 · 写完必须数一遍字数】
全文 80~150 字。★ 产品型实测语速约 **5.12 字/秒**（17 条样本），150 字 ≈ 29 秒。
★ 这一型句子最短、节奏最快（报菜名是连珠炮），所以**单句尽量压到 12 字以内**。

${SPOKEN_RULES}

【这一型额外的硬约束】
- ★★ **价格、菜名、套餐内容、赠品、叠加规则，一个字都只能从上面已给的信息里取**。
  套餐信息为空时：一份也不许编，改用骨架二（不报价的那一副）。
  编价格的后果最严重 —— 客人拿着视频来店里，一条就是一场纠纷
- 不许「超值」「划算到哭」这类评判词代替算账（把数字摆出来就够了）
- 不许编奖项、排队人数、销量
- 只输出文案正文：不要标题、不要分点、不要 hashtag、不要任何解释`

export const COPY_RECOMMEND_PROMPT = `你是餐饮短视频「种草型」文案专家，核心目标是【像一个真顾客在分享，让人觉得「这家我记下了」】。

${CONTEXT_BLOCK}

【这一型的定位 · 先认清它和「产品型」的区别】
这一型是**顾客视角**：我，一个来吃的人，告诉你我尝到了什么。
所以口径是「**我去**」而不是「**我这儿**」—— 后者是产品型，走的是店家视角。
★ 哪怕实际是老板自己拍的，这一型也必须装成一个食客：**用第一人称记录体验，不做推销动作**。
★ 但**不许编造体验**：素材只有上面已给的门店与菜品信息。写成「我前天去的」这种设定没问题，
  写「配菜有七种」而信息里没有，就是编 —— 那是会被老顾客当场戳穿的。

【口播骨架 · 先挑一副，再按它的顺序填】
**步骤顺序不许改** —— 四副都是从 16 条真种草向爆款逐句转写里拆出来的：

· 骨架一 · 探店全流程线（最主流）
  ① 由头或位置钩子：怎么找到这儿的（「拐个弯就看到」「跟着当地人来的」「开在菜市场边上」）
  ② 进门点菜：问一句招牌是什么，报自己要了什么
  ③ 逐道往下走，**每道都要带一口具体的口感**（吸满了汤汁 / 一抿就化 / 皮还是脆的）。
     ★ 只写口感，不写评判词
  ④ 一个细节故事：跟老板的一句对话，或店里某个具体的做法、某个老物件。
     这一段是这一型的价值所在，**不能省** —— 省了就退化成报菜名
  ⑤ 报一下总价，给一句值不值的直白判断。
     ★★ **只有在【套餐信息】里真的写了价格时才报数字**。那里为空（绝大多数情况 ——
     种草型是顾客视角，本来就很少拿到套餐表）⇒ 这一步改成「**一个数字都不许出现**，
     只说一句值不值的感受」。实测踩过：模型会自己编「这一大盘才几十块」，
     客人拿着视频来店里，一条就是一场纠纷。
  ⑥ 收尾一句推荐（「路过可以来试试」），不要喊「快冲」

· 骨架二 · 价格反差钩子线（价格特别便宜、或性价比是最大卖点时用）
  ★★ **【套餐信息】为空时整副不许用** —— 它的第①步就是一个具体数字，
     没给价格就只能靠编，而编价格是这一型最严重的错误。改用骨架一或骨架三。
  ① 反常识数字钩子（「七十一，随便吃」「十年了只卖十块」）
  ② 立刻接一句怀疑（「老板真不怕亏啊」）—— 这句话是把观众的怀疑先说出来
  ③ 眼见为实：扫一遍都有什么，**列出来，不要概括**（「毛肚自由了」「虾滑有好几种」）
  ④ 吃：逐口写口感
  ⑤ 回头回答开头那个疑问（把老板的原话或做法讲出来）
  ⑥ 升华成一句道理收尾（「小本生意拼的从来不是食材贵不贵」）
  ★ 第 ⑥ 步是这一副的灵魂，**不许省** —— 没有它，这条视频就只是一条便宜的广告

· 骨架三 · 反向钩子线（用一个反差开头抓住第一秒）
  ① 先说一句**反话**（「这家店我不会再来第二次了」）
  ② 第二句立刻解释（「因为下次来就是第三次」）
  ③ 逐道点评，每道一口口感
  ④ 收尾一句，短而干脆
  ⚠ 反话只能用一次，**不许**在一篇里放两个反向钩子

· 骨架四 · 做法拆解线（这家店的卖点是「做的过程讲究」时用）
  ① 反常识钩子开场（一个很普通的食材，做出了不一样的价）
  ② 拆解做法的每一步，**每一步都要带一句「为什么这么做」**
     （为什么要压扁、为什么要裹两层、为什么中间要晾三十秒）
  ③ 讲结果：口感上的差别到底在哪
  ④ 升华成一句道理收尾

一条文案只用一副骨架。**不许**把几副串起来用。

【篇幅 · 写完必须数一遍字数】
全文 90~170 字。★ 种草型实测语速约 **4.49 字/秒**（16 条样本）——
**这一型是四型里最慢的**，因为它天然带停顿、带咂摸的节奏。170 字 ≈ 38 秒，不要写更长了。

${SPOKEN_RULES}

【这一型额外的硬约束】
- ★ 不许编造：价格、奖项、排队人数、食材产地、绝对化结论（「全城最好吃」）。
  **【套餐信息】为空 ⇒ 全篇不许出现任何数字价格**（「才几十块」「不到一百」都算），
  也不许编「米饭管够」「配菜七种」这类内容清单 —— 素材里没有的就是没有
- ★ 不许制造虚假稀缺（「最后三天」）、不许喊麦
- ★ 不许用「探店播报」的口气（「今天带大家打卡一家」这种），要像跟饭搭子说话
- 评语必须落在具体的口感动作上，不许用「好吃」「绝了」「太香了」这种空词
- 只输出文案正文：不要标题、不要分点、不要 hashtag、不要任何解释`

export const STORY_PROMPT = `你是餐饮短视频分镜导演。把下面的口播文案拆成一份**一个人举着手机就能照着拍**的分镜脚本。
【门店】{{storeName}}｜品类：{{category}}｜城市：{{city}}
【门店介绍】{{storeIntro}}
【菜品】{{dishName}}｜卖点：{{sellingPoints}}
【套餐信息】{{comboInfo}}（空 = 这次推广的是单道菜，不要编造成套餐）
【门店人设】{{persona}}（有就自然用上，没有就不要编造）
【日期与节点】{{dateInfo}}
（上面这几项**任意一项为空 = 这条视频不涉及它，不要替它编造**。
 尤其注意：整块门店与菜品都为空时，说明这是一条纯话题视频，画面上就不该出现店名、招牌、
 菜品特写或菜单 —— 只拍人、环境、天气、日常，以及这句话题本身。）
【口播文案】{{copyText}}
【分镜复杂度】{{complexityLabel}}（{{complexity}}）
【分镜数量要求】{{shotCountRule}}
【可用镜头库 · 拍摄手法（libraryCode 必须从下表中选择）】
{{shotLibrary}}

【台词怎么切 · 这一条最容易做错】
line 必须**逐字**来自【口播文案】的连续片段：把所有 line 按顺序拼起来，要和原文**一字不差**地对上。
不许改写、不许润色、不许加字减字、不许调换语序 —— 那段文案是老板要照着念的，
你改了就等于替他改稿，用户拿到手会发现跟自己刚看到的不一样。
切分点选在**语义停顿**处（一句话说完、转折前后），不要把一个词从中间切开。

【画面怎么想 · 按「一个人能不能拍完」来写】
visualReq 要具体到**机位 + 动作 + 光线**：拍什么、从哪个角度、手怎么动、光从哪来。
必须满足「一台手机、一个人、不用额外设备」。下面这些一律不许出现：
航拍、摇臂、稳定器、滑轨、灯光组、多机位、演员、群演、特效、字体设计、后期调色。

【节奏】
第 1 个镜头要能让人**不划走** —— 出锅的响动、流心的一下、热气扑脸，或者老板一句直给的话。
最后一个镜头：有店名/地址就自然压住它；纯话题视频（上面门店为空）没有店可压，
就压在那句提问上，或者停在一个最有力的画面上，别硬加字幕。

输出要求：
1. 严格按【分镜数量要求】输出，镜头总数必须落在规定区间内
2. 每个分镜输出以下字段：
   - shotType：镜头分类，从 开场/口播/特写/原料/制作/环境/试吃/卖点/收尾 中选一个
   - shotSize：景别，从 远景/全景/中景/近景/特写/大特写 中选一个
   - durationSuggest：建议时长（整数秒，一般 2~6 秒）
   - line：从【口播文案】逐字截取的台词片段（切法见上面「台词怎么切」）
   - visualReq：画面要求，具体到机位、动作、光线，能照着拍（见上面「画面怎么想」）
   - libraryCode：从上方镜头库中选择最匹配的一条 libraryCode
3. 尽量覆盖这些基础拍摄手法：美食特写、老板口播、出锅、环境、原料、制作过程（复杂版/精细版应全部覆盖，简单版优先覆盖美食特写与老板口播）
4. 只输出 JSON 数组，不要 Markdown 代码块、不要任何解释文字`



// ── 兜底模板（AI 调用失败时用它渲染，不扣积分）──
// 取舍与四款正稿相反：兜底要**短**，所以不加 storeIntro、也不加 comboInfo（明细一长就超出口播长度）。
// ★ 同样**不能加 dateInfo**：兜底是静态文案，拿不到真实日期，写进节日只会让模型编一个出来。
// 但语气要和正稿一致 —— 兜底也是给老板照着念的，不能一边口语一边播报腔。
export const COPY_FALLBACK = `{{storeName}}的{{dishName}}，现做现卖。想吃就来，我在店里等你。`
// ★ 流量款兜底与其余四款不同：它跑在**话题模式**下，门店/菜品/城市三个变量都是空的，
//   所以这条模板**一个占位符都不能用**（否则渲染成空串会读出「◯◯的」这种断句），
//   也不能提季节或节日（兜底拿不到日期，写了就等于让老板念一个错的时间点）。
//   内容只保留「一句能让人接话的提问 + 自嘲」这个最小骨架。
export const COPY_TRAFFIC_FALLBACK = `你上一回好好坐下吃顿饭，是啥时候？我最近净凑合了。你们平时都怎么解决的，说一声。`
// ★ 人设型兜底：这一型**不报价、不报菜**，所以兜底也只留「我是谁 + 一件具体的事 + 立场」。
//   `storeIntro` / `persona` 都**不加**进兜底 —— 它们可能是一大段，兜底一长就超出口播长度。
//   但 `storeName` 与 `city` 可以留：人设型本来就要交代「在哪儿做什么的」。
export const COPY_PERSONA_FALLBACK = `我是{{city}}{{storeName}}的老板。这行干了有些年头了，东西都是当天备、现做现卖。你们那儿的老板也这么干吗？`
// ★ 干货型兜底：这一型**不许出现自家店名/菜名**，所以这条模板一个门店占位符都不能用
//   （用了就等于让老板念一个自家店名出来，当场破了这一型唯一的硬规矩）。
//   内容只保留「抛一个反常识问题 + 给两条判据 + 急停」这个最小骨架。
export const COPY_KNOWLEDGE_FALLBACK = `为什么在家做菜总没有饭店香？油温不到位是一个，料下得太早也是一个。这两样对了，味道就上来了。`
// ★ 产品型兜底：这一型要的是算账式的清楚，但兜底拿不到真实价格与套餐明细，
//   所以只写「有什么 + 怎么做 + 来尝尝」，**一个数字都不许出现**（编价格后果最严重）。
export const COPY_PRODUCT_FALLBACK = `{{storeName}}的{{dishName}}，{{sellingPoints}}。分量实在、做法明摆着，你到店里点一份尝尝就知道。`
export const COPY_RECOMMEND_FALLBACK = `朋友带我去的{{storeName}}，点了{{dishName}}，吃完我就记下了。你要是路过{{city}}，可以去试试。`
/**
 * ★ 分镜兜底与文案兜底不同：它的 line 也必须**能念**。
 *
 * 旧版是三镜骨架，line 分别是「{{dishName}}，现做现卖」「就在{{city}}{{storeName}}」「欢迎到店品尝」。
 * 加了话题稿（`mode='TOPIC'`）之后那三个变量**全是空串**，渲染出来就是
 * 「，现做现卖」「就在」「欢迎到店品尝」—— 用户看到的是半句话，而这条路径不报错、也不提示降级。
 * 一个模板要同时服务菜品稿与话题稿，就不能依赖那三个变量。
 *
 * 改成**单镜、line 直接是整条文案**（`{{copyText}}`）：
 *   · 对两种模式都成立 —— copyText 是话题稿与菜品稿都有的那个变量
 *   · 「把所有 line 拼起来 == 原文」这条逐字契约在兜底上也**自动成立**（只有一条）
 *   · 退化形态是「一条拍完，照着念」，诚实可用；比三镜里有两句是断句强得多
 * ⚠ 放弃三镜骨架是权衡后的取舍：三镜更"像分镜"，但它在话题稿上必然是坏的，
 *   而兜底本来就是「AI 全挂了」时的最低保障，正确性优先于丰富度。
 */
export const STORY_FALLBACK = `[{"shotType":"口播","shotSize":"近景","durationSuggest":6,"line":"{{copyText}}","visualReq":"老板举着手机自拍式口播，一条拍完，店内自然光，正脸为主","libraryCode":"boss_talk"}]`

/**
 * ★★ `beanPrice` 为什么必须写在这里（2026-09-21 四款改型时补的）：它同时是
 *   「单次扣费硬上限」与「预冻结额」，而 `ai-prompts:sync` 在**新建**一行时用的是
 *   这份清单里的值。四款改型新建了 3 个场景，当时清单里没给价 ⇒ 建出来是
 *   `bean_price = 0` ⇒ `charged = min(wantCharge, 0) = 0` —— **每次生成免费**，
 *   而且不报错、日志里也看不出异常，只有对账时才发现少收钱。
 *   所以「新增一个场景」必须连带想清楚它的价，不能留空。
 * ★ 这张表与 `scripts/setup-ai-channels.ts` 的 `SCENE_CAPS` 是**同一件事**
 *   （那张表已改成从这里取值），要改价请改这里，改完重跑 `ai-channels:setup` 复核。
 */
export const CREATION_SCENE_PROMPTS = [
  { code: 'copy_generate', name: '短视频文案生成（通用·兼容旧客户端）', prompt: COPY_PROMPT, fallback: COPY_FALLBACK, temperature: 0.8, beanPrice: 70 },
  { code: 'copy_traffic', name: '文案 · 流量款（话题驱动：节气节日/时令/生活共识，无门店无菜品）', prompt: COPY_TRAFFIC_PROMPT, fallback: COPY_TRAFFIC_FALLBACK, temperature: 0.9, beanPrice: 80 },
  { code: 'copy_persona', name: '文案 · 人设型（讲人：立场/经历/自黑/情绪，不报价不荐菜）', prompt: COPY_PERSONA_PROMPT, fallback: COPY_PERSONA_FALLBACK, temperature: 0.85, beanPrice: 80 },
  { code: 'copy_knowledge', name: '文案 · 干货型（讲这行的知识：怎么做/怎么挑/怎么避坑，不推自家店）', prompt: COPY_KNOWLEDGE_PROMPT, fallback: COPY_KNOWLEDGE_FALLBACK, temperature: 0.8, beanPrice: 80 },
  { code: 'copy_product', name: '文案 · 产品型（店家视角：有什么/多少钱/值不值）', prompt: COPY_PRODUCT_PROMPT, fallback: COPY_PRODUCT_FALLBACK, temperature: 0.8, beanPrice: 80 },
  { code: 'copy_recommend', name: '文案 · 种草型（顾客视角：我去吃了/我尝到了什么）', prompt: COPY_RECOMMEND_PROMPT, fallback: COPY_RECOMMEND_FALLBACK, temperature: 0.85, beanPrice: 60 },
] as const

/** 分镜场景的模板（复杂度/镜头数与文案场景不同，单独配置） */
export const STORYBOARD_SCENE = {
  code: 'storyboard_generate',
  name: '分镜脚本生成（按复杂度 2~9 镜 + 镜头库匹配）',
  prompt: STORY_PROMPT,
  fallback: STORY_FALLBACK,
  temperature: 0.7,
} as const

// ────────────────────────── 发布素材（2026-09-21 新增） ──────────────────────────
//
// 需求原话：「增加一个生成发布素材（标题、封面、文案）功能，根据口播文案信息去生成，
//            封面默认 3:4 竖屏，生图模型用 GPT-Image2」。
//
// ★ 为什么要拆成**两个场景**（文本 + 图像）而不是一个：
//   1. 协议不同。文本走 `chat/completions`，出图走 `images/generations`，
//      在网关里是两个适配器、两种候选模型（`capability` 也不同）。
//      合成一个场景就没法表达「用哪个模型干什么」。
//   2. 计费口径不同。文本按 token 成本结算，出图是**按张固定价**
//      （返回里没有 token 用量，见 adapters.ts 的 openaiImage 说明）。
//      硬塞进一个场景，两笔账都算不清。
//   3. 失败可以分开承担：文案出来了、封面挂了，用户至少还有标题和文案可用，
//      而不是整单作废。所以封面单独一个场景、也单独可重试。
//
// ★ 链路是「文本场景产出 coverPrompt → 图像场景渲染它」：
//   出图的画面要求由**懂这家店的人**（文本模型，它看得到门店/菜品/口播文案）来写，
//   图像场景只负责把画风与规格钉住（3:4、留白、不许出现文字）。
//   反过来让图像场景自己去读门店信息，等于把「读业务上下文」和「控画面规格」
//   混在一份提示词里，两边都做不好。

/**
 * 发布素材 · 文本部分（标题 + 文案 + 封面画面的写法）。
 *
 * ★ 输入里**必须**有口播文案（`{{copyText}}`）：这一条的验收标准就是
 *   「标题/文案 ≤ 口播文案说过的范围」—— 口播里没提的优惠、地址、电话，
 *   标题和文案里也不许出现。用户报过的原话是「根据口播文案信息去生成」，
 *   所以这段文案是**唯一事实来源**，不是参考。
 */
export const PUBLISH_MATERIAL_PROMPT = `你是短视频发布运营，负责把一条已经剪好的餐饮短视频，包装成能直接发出去的一条内容。

${CONTEXT_BLOCK}
（以上门店/菜品信息**为空就当作这条视频不涉及它**，不要替它编造。
 没有菜品信息时，标题与文案不要出现具体菜名。）
【口播文案 · 这条视频里老板实际说出口的话】
{{copyText}}

【只输出这三样】
title        —— 视频标题（发布时填在标题栏）
caption      —— 发布文案（正文/简介，可以带话题标签）
coverPrompt  —— 封面图的画面描述（写给图像模型看的，见下面「封面怎么写」）

【title 怎么写】
- 12~22 个字，前 6 个字就要有钩子（人、动作、或一个具体的数字）
- 必须是**口播里说过的事**的提炼，不许加口播里没有的承诺
- 允许 0~1 个 emoji，不许堆表情
- 不许用这些词：最、第一、绝对、唯一、史上、必吃榜、天花板
- 不许写门店地址、电话、优惠力度（口播没说的就别写）

【caption 怎么写】
- 80~180 字，分 2~4 个短段（每段 1~2 句），手机上读着不累
- 内容和口播文案**同源**：可以重新组织，但不许添加新的事实（价格、活动、招牌年数等）
- 最后单独一行放 3~5 个话题标签，形如 #廊坊美食 #现做现卖（没有城市信息就用通用标签，
  并且**不要编造城市名**）
- 同样受口语化铁律约束：不用「匠心」「唇齿留香」这类书面词

【coverPrompt 怎么写 · 这段会被直接送去出图】
- 用**一句中文**写清画面，按这个顺序给信息：主体（拍什么）→ 状态（什么动作/瞬间）
  → 环境（在哪、背景是什么）→ 光线与色调 → 构图要求
- 主体优先选**食物本身**（口播里提到的那个菜、那道工序、那口锅），
  其次是门店环境或人的动作；不要把「文字排版」写进去
- 必须包含这两句要求：竖版 3:4 构图、主体居中偏上且四周留白
- 必须写明：画面里不要出现任何文字、水印、logo
- 不要出现清晰可辨的人脸；需要人时用「手正在……」「老板背影」这类表述
- 60~120 字，不要写成一段故事，就写「画面里有什么」

【硬约束】
1. 只输出一个 JSON 对象，键名**必须**是 title / caption / coverPrompt，不要多也不要少
2. 不要 Markdown 代码块、不要解释、不要前后缀文字
3. caption 里可以有换行（写成 \\n），title 与 coverPrompt 都不许有换行
4. 口播文案为空时，只根据门店与菜品信息写；门店也空时，写一条不讲具体店与菜的通用内容`

/**
 * 兜底模板：AI 失败时用它，不扣积分。
 *
 * ★ 这里刻意**一个门店/菜品占位符都不用**。原因与 STORY_FALLBACK 那条一模一样：
 *   流量款（`mode='TOPIC'`）的门店与菜品是空串，`"{{storeName}}的{{dishName}}"` 会渲染成
 *   「的」这种断句，而这条路径不报错、也没有任何提示。
 *   所以标题与文案都写成**与门店无关的通用句**，文案正文留给服务端用 copyText 补
 *   （见 publish-material.service.ts：caption 为空时复用口播文案）——
 *   那是唯一在两种模式下都非空的变量。
 *
 * ★ 必须是**合法 JSON**：解析失败会退化成「整单失败」，而这个模板的存在意义就是别失败。
 */
export const PUBLISH_MATERIAL_FALLBACK = `{"title":"今天这道菜，值得专门来一趟","caption":"","coverPrompt":"中餐招牌菜特写，刚出锅冒着热气，暖色调侧逆光，背景是虚化的暖黄灯光堂食环境，竖版 3:4 构图，主体居中偏上、四周留白，画面中不要出现任何文字、水印与 logo"}`

/**
 * 发布素材 · 封面图（图像场景）。
 *
 * ★ 这份模板里**只有规格与画风**，没有门店/菜品的任何占位符：
 *   具体拍什么由文本场景产出的 {{coverPrompt}} 决定（见上面的说明）。
 *   两段职责分开之后，运营在后台调这份模板就能整体换风格（比如从「暖黄烟火气」
 *   换成「干净日系」），而不必碰每个场景的业务提示词。
 */
export const PUBLISH_COVER_PROMPT = `{{coverPrompt}}

【出图规格 · 逐条都要满足】
- 竖版，宽高比 3:4
- 单幅画面，不要拼图、不要多格、不要边框、不要贴纸
- 商业美食摄影质感：真实、干净、有食欲；不要插画感、不要 3D 渲染感
- 暖色调，自然光或店内暖光；不要冷蓝调
- 主体居中偏上，四周留出呼吸空间，主体不贴边、不被裁切
- ★ 画面里**不要出现任何文字**：不许有中文、英文、数字、价格、水印、logo、菜单字幕
  （文案与标题由发布时另外填写，画面里重复一遍只会出现错别字或被平台判为硬广）
- 不要出现清晰可辨的人脸`

export const PUBLISH_COVER_FALLBACK = `中餐招牌菜特写，刚出锅冒着热气，暖色调侧逆光，背景虚化的暖堂食环境。竖版 3:4 构图，主体居中偏上、四周留白，画面中不要出现任何文字、水印与 logo`

/**
 * 发布素材的两个场景（文本 + 图像），供 seed 建行与 ai-prompts:sync 洗库共用。
 *
 * ★ `kind` 必须写在这里（而不是靠 seed / setup 脚本各自硬编码）：
 *   它决定网关走哪个协议（chat/completions 还是 images/generations），
 *   两处各写一遍就是「改了一处、另一处还是旧值」的经典静默错配。
 *
 * ★ `beanPrice` 也写在这里，因为**建行时就必须带着正确的价**：
 *   出图场景走固定价结算，若建行时 beanPrice=0，那么每一次出图的实收就是 0 积分 ——
 *   上游照付钱、用户白拿图，而且**不会报任何错**。
 *   `setup-ai-channels.ts` 的 SCENE_CAPS 会直接引用这里的值（不另抄一份数字）。
 */
export const PUBLISH_SCENES = [
  {
    code: 'publish_material',
    name: '发布素材 · 标题/文案/封面画面描述（文本）',
    kind: 'TEXT',
    prompt: PUBLISH_MATERIAL_PROMPT,
    fallback: PUBLISH_MATERIAL_FALLBACK,
    temperature: 0.85,
    /**
     * 单次上限（文本场景是**安全网**，按 token 成本结算、恒不超过它）。
     *
     * 依据（2026-09-21 首次真实跑通的实测值）：
     *   输入 5510 token / 输出 341 token（输入大是因为与 copy_* 共用同一个 CONTEXT_BLOCK ——
     *   storyboard_generate 实测 5321，体量同族，不是这条场景特有）
     *   ⇒ 生产单价下（gpt-5.5: in 2880 / out 17280 分每百万）
     *     costFen = ceil(5510×2880/1e6) + ceil(341×17280/1e6) = 16 + 6 = **22 分**
     *   ⇒ 应扣 = 22 × 100 × 4 / 100 = **88 积分**
     *
     * ⚠ 所以**不能**用 100：那只剩 1.5 倍余量，而本项目的输出 token 波动极大
     *   （同一提示词连打 3 次成本能差 15 倍，变量是「思考 token」，见 setup-ai-channels.ts 文件头）。
     *   按既有规则「实测应扣 × 2 取整到 10」⇒ 88 × 2 = 176 ⇒ **180**。
     *   换个说法：输入固定吃掉 16 分，180 积分（= 45 分）留给输出 29 分 ≈ 1678 个输出 token，
     *   是实测 341 的 5 倍。
     * ⚠ 上限同时是**预冻结额**。180 会让「账户可用积分不足」的门槛抬高到 180
     *   （封面那条是 300，两步是先后发生的，峰值按 300 算而不是 480）。
     */
    beanPrice: 180,
    timeoutMs: 30000,
    maxRetries: 1,
    maxOutputTokens: 4000,
  },
  {
    code: 'publish_cover',
    name: '发布素材 · 封面图（图像，3:4 竖版）',
    kind: 'IMAGE',
    prompt: PUBLISH_COVER_PROMPT,
    fallback: PUBLISH_COVER_FALLBACK,
    temperature: 0.6,
    /**
     * ★★ 出图场景的 beanPrice **不是上限，就是报价本身** ——
     *    图像场景走固定价结算（`AiChargeParams.fixedBeans`），因为出图返回里
     *    **没有 token 用量**（实测 `usage: null`），按 token 成本算恒为 0。
     *    所以它直接等于用户点一次「生成封面」花掉的积分。
     *
     * 定价依据（2026-09-21 实测，可原样复核）：
     *   ① 上游单价：tokenbox 计费表 `gpt-image-2` 的 `model_price = 0.10`（USD/张）。
     *      ★ 这个 0.10 是**真的量出来的**，不是抄价目表：
     *        `GET /dashboard/billing/usage` 的 `total_usage`（美分）在生成一张前后
     *        从 84 → 94，即 $0.10。注意出图 route 里 `resolution_tier` 报的是 `1k`，
     *        但计费跟的是**请求的模型名**而不是这一档（所以别指望按 1k 的价付钱）。
     *   ② 美元 → 人民币：0.10 × 7.2 = ¥0.72 = **72 分**
     *   ③ 分 → 积分：与文本同一把尺子
     *      `ceil(costFen × points_per_yuan × cost_multiplier / 100)` = 72 × 100 × 4 / 100 = **288**
     *   ④ 取 **300**：向上取整到 10，且不会因汇率小幅波动而低于成本。
     *
     * ⚠ 换模型 / 换尺寸都要重算（`TB_IMAGE_MODEL` / `AI_IMAGE_SIZE`）：
     *     gpt-image-2       @1024x1365  → 实测 $0.10 → 288 → **300**（当前）
     *     gpt-image-2-1k    @768x1024   → 价目 $0.06 → 172 → 180
     *       （★ 该变体**拒绝** 1024x1365：`exceeds channel resolution limit 1k`，
     *        而且它返回的是 b64 而不是 URL，比裸码慢一倍：实测 91s vs 45s。
     *        想省钱就把 TB_IMAGE_MODEL / AI_IMAGE_SIZE 一起换成这两个值，
     *        并把这里的 beanPrice 改成 180 —— 三者必须同时改，只改一个就会对不上账。）
     * ⚠ 汇率变了（TB_USD_TO_CNY）也要重算。固定报价不随成本浮动，
     *   上游涨价时差额由平台承担 —— 这是选「固定价」的代价，不是 bug。
     */
    beanPrice: 300,
    // 实测单张 22~45s（含排队），给 90s 留一倍余量
    timeoutMs: 90000,
    // 出图按张付费，重试 = 重复付钱；且失败多是上游硬故障，立刻重试不会好
    maxRetries: 0,
    // 图像路径不发 max_tokens（走 images/generations，不是 chat/completions），此值仅占位
    maxOutputTokens: 4000,
  },
] as const

// ─────────────────────────────────────────────────────────────────────────────
// AI 剪辑决策（EDL）—— 2026-09-22 新增
//
// ★ 它是本项目**第一个「模型直接驱动剪辑工具」的场景**：此前 AI 只写文案与分镜，
//   「怎么剪」完全由用户在面板上选的固定档位决定（转场 3 档 × 节奏 3 档 × …），
//   出片永远像模板套的 —— 用户的原话是「没有转场和剪辑」。
//   本场景让模型拿到**逐镜头的台词与素材时长**，直接给出剪辑参数。
//
// ★ 输出被 `src/render/edl.ts::parseEdl` 解析，**只认 JSON**：
//   解析失败一律退回用户档位（绝不阻塞出片）。所以这里的 JSON 结构必须与它一致 ——
//   改字段名要两边一起改，否则会静默退化成「永远按档位剪」而**不报任何错**。
// ─────────────────────────────────────────────────────────────────────────────

export const EDIT_PLAN_PROMPT = `你是资深短视频剪辑师。下面这条片子的素材已经拍好、配音稿也写好了，
你的任务不是写文案、也不是改台词，而是**决定这条片子该怎么剪**。

【逐镜头素材与台词】
{{shotPlanInput}}

【用户在面板上选的倾向】{{preferPlan}}
【用户备注】{{note}}

输出要求：
1. shots：为**每一个镜头**给出目标时长 targetMs（毫秒，整数），index 从 0 开始，必须与上面的镜头编号一一对应、**一个都不能少**。
   · 判断依据是这句台词的信息量与长度：交代信息的长句给 3000~4500；短促有力的句子（如「就是这一口」）给 1200~2000。
   · **绝不能超过该镜头的素材时长**，并请留出约 10% 余量（转场要占用两侧素材）。
   · 时长要有**起伏**：所有镜头给同一个数字等于没剪；相邻镜头尤其不要雷同。
2. pacing（整体节奏）：NATURAL=自然叙述｜FAST=快切，适合探店/种草｜STORY=舒缓，适合品牌故事
3. transitions（转场）：CLEAN=硬切，最利落｜SMOOTH=柔和叠化，适合温馨叙事｜DYNAMIC=甩镜动感，适合强节奏
4. subtitleStyle（字幕样式）：CLEAN=基础白字｜EMPHASIS=黄字黑边，促销强调｜SOCIAL=社媒卡片风
5. bgm（配乐）：NONE=不要｜LIGHT=温馨轻音乐｜UPBEAT=轻快电子｜PREMIUM=高级弦乐钢琴
6. reason：每个镜头一句话说明取舍理由，不超过 20 字
7. **只输出 JSON 对象**，不要 Markdown 代码块、不要任何解释：
{"pacing":"","transitions":"","subtitleStyle":"","bgm":"","shots":[{"index":0,"targetMs":3000,"reason":""}]}`

/**
 * 兜底模板。
 *
 * ★ `shots` 故意留空 —— 网关走兜底时 `edit-plan.service.ts` 会先看 `isFallbackTemplate`
 *   直接退回档位，根本不会拿它去解析；但万一有人改了那条判断，空 shots 也会让
 *   `parseEdl` 返回 null（它的判据就是「一个可用镜头时长都没解析出来」）
 *   ⇒ 仍然退回档位。**两道保险都不改出片结果**，这是有意的冗余。
 */
export const EDIT_PLAN_FALLBACK = `{"pacing":"NATURAL","transitions":"SMOOTH","subtitleStyle":"CLEAN","bgm":"NONE","shots":[]}`

export const EDIT_PLAN_SCENE = {
  code: 'edit_plan',
  name: 'AI 剪辑决策 · 逐镜头时长与整体节奏（文本）',
  kind: 'TEXT',
  prompt: EDIT_PLAN_PROMPT,
  fallback: EDIT_PLAN_FALLBACK,
  temperature: 0.5,
  /**
   * 单次上限（文本场景是**安全网**：按 token 成本结算，恒不超过它，超出由平台承担）。
   *
   * ⚠ **这个数字是估算，尚未实测**（与 publish_material 那种「算完再定」的不同）：
   *   本场景的输入只有逐镜头清单与倾向说明（预计 600~900 token，远小于
   *   publish_material 的 5510），输出是一份 JSON（6 镜头约 300~400 token）。
   *   按生产单价（gpt-5.5: in 2880 / out 17280 分每百万）估：
   *     输入 ~800 token → ceil(800×2880/1e6) = 3 分
   *     输出 ~400 token → ceil(400×17280/1e6) = 7 分
   *     合计 10 分 ⇒ 应扣 = 10 × 100 × 4 / 100 = **40 积分**
   *   沿用既有规则「实测应扣 × 2 取整到 10」⇒ 80；考虑到**思考 token 的波动可达 15 倍**
   *   （见 setup-ai-channels.ts 文件头），取 150 留出约 3.7 倍余量。
   *   ★ 真跑一次后应回来按实测值校准，并把上面的「估算」改成实测口径。
   * ★ 输出预算抬到 4000 之后，理论最坏（思考与正文吃满）约 73 分 ⇒ 292 积分；
   *   超出本上限的部分由平台承担 —— 这正是「安全网」的语义，与其它文本场景一致。
   */
  beanPrice: 150,
  /**
   * ★★ `timeoutMs` / `maxRetries` / `maxOutputTokens` 与
   *   `scripts/setup-ai-channels.ts` 的 `SCENE_OVERRIDES.edit_plan` **必须同值**。
   *
   *   分工：`ai-prompts:sync` 只在**建行时**写这三个字段，update 分支一概不碰；
   *   `ai-channels:setup` 用 SCENE_OVERRIDES 覆盖它们。两处不一致的后果是
   *   「手工重建一次场景，配置就悄悄退回另一套」—— 没有任何地方会报错。
   *
   * ★ 90s / 0 重试是**实测口径**（2026-09-22 上线首跑，任务 15）：gpt-5.5 单次 46.6s
   *   （prompt 5097 token），60s 预算只剩 29% 余量 ⇒ 镜头一多必然超时。
   *   完整依据与「为什么还要补一个 DeepSeek 备用」写在 SCENE_OVERRIDES 那一段。
   */
  timeoutMs: 90_000,
  maxRetries: 0,
  /**
   * ★★ 必须 ≥ `SCENE_MIN_OUTPUT_TOKENS`（默认 4000），**不是**「JSON 只要 400 token 就给 1200」。
   *
   * 理由与 `setup-ai-channels.ts` 文件头第 4 条同一口径：推理模型把 `max_tokens` 当作
   * **思考 + 正文共用**的预算。1200 会被思考吃光 ⇒ 返回 `finish_reason='length'` 且
   * `content=''`，而 HTTP 200、报文结构完全合法。网关把空正文判成 `BAD_RESPONSE`，
   * 那是**非通道级**故障 ⇒ 它会在**同一个通道上**按 `maxRetries` 反复重试（这里 1 ⇒ 白等 2 次），
   * 最后抛错被 `generateEditPlan` 接住 ⇒ 表现是「**每一次 AI 档出片都静悄悄按面板档位剪**」，
   * 不留任何报错。（2026-09-22 复核时改的：原值 1200 只算了「输出 400 × 3 倍」，漏了思考预算。）
   *
   * ★ 它是**安全网**而不是目标值：结算按实际 token 成本走，正常输出仍只有 300~400 token。
   * ★ 本场景**不在** `LOW_REASONING_SCENES` 里（v1 不压思考预算）。要往里加，必须先按
   *   `scene-codes.ts` 那张表的口径在生产真打一轮、拿到「成功 / 耗时 / 正文长度」再定 ——
   *   ⚠ 尤其 `claude-sonnet-5` 完全无视 `reasoning_effort`，不能进这类场景的候选链。
   */
  maxOutputTokens: 4000,
}
