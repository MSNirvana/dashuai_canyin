# 大帅餐饮短视频创作工具 · 架构方案要点（**定稿 v3 · 历史底稿**）

> ⚠ 本文档为早期需求/合同底稿，仅供溯源。当前有效方案请优先看：
> - `docs/当前有效方案.md`
> - `docs/审计-产品体验.md`
> - `docs/审计-架构与业务逻辑.md`
> - `docs/审计-QA验证.md`
> - `docs/system_design.md`

> 需求依据：《软件开发服务合同》正文 + 附件一功能清单 + 附件二第三方费用表
> v3 变更：确认 1 元 = 100 豆；确认"导出成片必须会员"；会员定价 99/199/599；**交付策略调整为一期微信小程序、抖音端排二期**
> 状态：**框架已定稿**，可进入详细设计（页面流程 + 接口定义 + 排期拆分）
> ⚠ 第零节的合同差异仅为提醒，按甲方决策执行，不影响本方案

---

## 零、合同差异提醒（仅备案，按甲方决策执行）

| # | 项 | 合同现状 | 甲方决策 |
|---|---|---|---|
| 1 | 多门店 | 附件一排除清单含"多门店连锁" | 按多门店做 |
| 2 | AI豆充值 + 按量计费 | 附件一无此体系 | 做 |
| 3 | 会员 8 折购豆 | 附件一无 | 做 |
| 4 | 镜头库文案 AI 生成 | 模块三写"从预置镜头库匹配" | 做 |

**建议**：四项打包成一份变更单提交乙方（合同 5.4 + 14.3，微信/邮件书面确认即生效），一次性谈价，避免被拆成四次报价。

**有利发现**：合同 3.2 第三期（20%）、第四期（10%）的触发条件均写的是"**至少一个平台**上线/达到上线提交条件"。因此**一期只上微信小程序即可触发全部付款节点**，先做微信端在合同上完全站得住。建议在排期表中书面明确"抖音端为二期"，双方都受保护。

---

## 一、技术选型

| 项 | 选择 |
|---|---|
| 跨端框架 | Taro 4 + React 18 + TypeScript（条件编译 `process.env.TARO_ENV`） |
| 后端 | Node.js 20 + TypeScript（NestJS 或 Fastify） |
| 数据库 | MySQL 8（InnoDB）+ Prisma |
| 缓存 / 队列 | Redis + BullMQ |
| 对象存储 | 腾讯云 COS + CDN（前端直传） |
| AI | DeepSeek API（甲方注册充值） |
| 视频合成 | 服务端 FFmpeg（静态编译版，含 libx264） |
| 运营后台 | React + Ant Design，独立 PC Web，**单角色全权限** |
| 部署 | Nginx + PM2 + Docker Compose，国内云服务器（建议 8 核 16G） |

---

## 二、交付与排期策略（v3 新增）

| 期次 | 范围 | 说明 |
|---|---|---|
| **一期（主线）** | 微信小程序端**完整闭环**：模块一~五 + AI豆 + 会员 + 微信支付 | 全部业务逻辑在微信端跑通，可触发合同第三、四期付款 |
| **二期** | 抖音小程序端：模块一~四（录入/文案/分镜/粗剪），支付与 AI豆充值视平台能力决定 | 一期验收后启动；抖音端支付不通时隐藏充值入口，或引导至微信端 |

- 一期即按多门店、AI豆账务完整实现，避免二期返工
- 平台适配层在一期就写好，抖音端只做适配实现，不重写业务
- **一期必做**：第一周做抖音端真机冒烟（选素材 → 上传 → 合成 → 保存），提前暴露平台能力问题，别等二期才发现

---

## 三、代码仓库结构

```
dashuai-video/                    # 单仓库，pnpm workspace
├── apps/
│   ├── mini/                     # Taro 小程序（→ weapp 一期 / tt 二期）
│   └── admin/                    # 运营后台 PC Web（单角色全权限）
├── server/
│   ├── src/modules/              # auth / merchant / store / dish / media
│   │                             # ai / storyboard / render
│   │                             # bean(账务) / member / order
│   ├── src/platform/             # 微信/抖音差异适配
│   ├── src/jobs/                 # FFmpeg 合成任务消费者
│   └── prisma/schema.prisma
└── packages/
    ├── shared/                   # DTO、枚举、错误码
    └── platform-adapter/         # 小程序端：登录/上传/支付/消息/保存
```

---

## 四、页面与分包规划

| 分包 | 页面 | 说明 |
|---|---|---|
| **主包** | login、home（门店切换 + 创作入口 + 作品列表）、mine | 目标主包 < 1.2MB |
| **creation** | creation（选门店/菜品/赛道/复杂度）、script（文案 + 分镜）、shoot（按槽位拍传 + 示范视频） | 进首页 preload |
| **render** | render（调色 + 合成进度 + 预览 + 导出） | **会员卡点在此** |
| **bean** | bean（余额 + 充值档位 + 消费明细） | |
| **member** | member（订阅 + 权益对比 + 支付） | |
| **profile** | store-list、store-edit、dish、persona | 多门店管理 |

- 全局 `currentStoreId`（storage + store），素材/创作/统计按门店隔离
- 资源全部 CDN 化，图标 SVG / iconfont，不进包

---

## 五、核心数据模型

**业务实体**

| 实体 | 关键字段 |
|---|---|
| merchant | id, phone(唯一), status, **registered_bean_granted** |
| store | merchant_id, name, category, region, contact, is_default, status |
| dish | store_id, name, intro, selling_points, sort |
| persona | merchant_id, boss_tags, activity_info |
| media_asset | store_id, owner_type/owner_id, type, cos_key, size, duration, w/h, status |
| creation | merchant_id, store_id, dish_id, track, complexity, copy_text, status |
| shot | creation_id, index, shot_type, duration_suggest, line, visual_req, library_shot_id, asset_id, trim_start, trim_end |
| shot_library | code, name, category(六类), tips, source(ai/manual), demo_video_key, editable |
| render_task | creation_id, status, params(亮度/对比度/饱和度/色温), progress, result_key, preview_key, error, cost_ms |

**账务实体（AI豆）**

| 实体 | 关键字段 |
|---|---|
| bean_account | merchant_id, balance, **grant_balance(赠豆)**, frozen, total_recharge, total_consume, version |
| bean_ledger | merchant_id, type(recharge/grant/freeze/consume/unfreeze/refund/adjust), amount, **bucket(recharge/grant)**, balance_after, biz_type, biz_id, request_id(UNIQUE), created_at |
| bean_package | beans, price_fen, member_price_fen, bonus_beans, enabled, sort |
| ai_call_log | merchant_id, biz_type, model, prompt_tokens, completion_tokens, cost_fen, bean_charged, request_id, latency_ms |

**会员与订单**

| 实体 | 关键字段 |
|---|---|
| member_package | name, price_fen, duration_days, **grant_beans**, rights_json, enabled |
| membership | merchant_id, package_id, start_at, end_at, source_order_id |
| order | order_no, merchant_id, order_type(RECHARGE_BEAN / MEMBERSHIP), ref_id, amount_fen, member_discount_applied, status, wx_transaction_id, paid_at |

---

## 六、AI豆计费体系

### 6.1 换算与标价（已确认）

- **1 元 = 100 豆**，会员 8 折即 1 元 = 125 豆
- 定价模型：**明码按次 + 成本监控**（"实际成本 × 系数"作为定价依据与毛利监控，不暴露给用户）

| 功能 | 标价 | 估算实际成本 | 毛利 |
|---|---|---|---|
| 生成口播文案 | 5 豆 | ≈ ¥0.01 | ~80% |
| 生成分镜脚本 | 10 豆 | ≈ ¥0.02 | ~80% |
| 单条文案重写 | 5 豆 | ≈ ¥0.01 | ~80% |
| 镜头库手法 AI 生成（后台） | 不扣豆 | 一次性 | — |

充值档位（建议，可调）：¥10→1000 豆 ｜ ¥50→5000+500 ｜ ¥100→10000+1500。会员 8 折，赠豆不变。

### 6.2 会员套餐（已确认）

| 套餐 | 价格 | **建议赠豆** | 赠豆成本 | 权益 |
|---|---|---|---|---|
| 月卡 | ¥99 | 1,000 豆 | ≈ ¥2 | 导出成片 + 8 折购豆 + 优先队列 |
| 季卡 | ¥199 | 3,500 豆 | ≈ ¥7 | 同上 |
| 年卡 | ¥599 | 15,000 豆 | ≈ ¥30 | 同上 |

> 赠豆建议的理由：若会员付了 99 还要另买豆，心理阻力很大。赠豆成本占比极低（月卡 2%），但转化提升明显。

**赠豆规则（建议）**
- 赠豆与充值豆**分账存储**（`bucket` 字段），消费时**优先扣赠豆**
- 赠豆随会员周期发放，**会员到期未用完清零**
- 充值豆不过期

**新用户注册赠豆（建议）**：送 30 豆（可生成 3 次文案），成本 ≈ ¥0.06/人，`registered_bean_granted` 防重复发放。

### 6.3 扣费时序（硬性约束）

1. 前端生成 `requestId`（UUID）→ `POST /ai/copy { requestId, ... }`
2. 校验余额 ≥ 标价 → **冻结**（ledger `freeze`，先冻赠豆再冻充值豆）
3. 调 DeepSeek，tokens 与实际费用写入 `ai_call_log`
4. 成功 → 按实际成本结算扣减、差额解冻；失败/超时 → **全额解冻，不扣豆**
5. ledger 写入 + account 更新**同一事务**

**三条不能破的规则**
- **幂等**：`bean_ledger.request_id` 唯一索引，重复提交返回首次结果（用户连点、网络重试必现）
- **原子扣减**：`UPDATE bean_account SET balance=balance-:amt, frozen=frozen+:amt, version=version+1 WHERE merchant_id=? AND balance>=:amt AND version=?`，绝不先读后写
- **不追加扣费**：预冻结上浮 50% 作 buffer，结算取 `min(冻结额, 实际×K)`，超出由平台承担并记入成本报表

### 6.4 权益矩阵

| 能力 | 非会员 | 会员 |
|---|---|---|
| 录入门店/菜品/人设 | ✅ | ✅ |
| 多门店 | ✅（建议限制 1 家，待定） | ✅（N 家，待定） |
| AI 生成文案/分镜 | ✅ 扣豆（原价） | ✅ 扣豆（8 折购豆） |
| 按槽位上传素材 | ✅ | ✅ |
| 调色 + 合成 | ✅ | ✅（队列优先） |
| **预览** | 低清预览（待定规格） | 完整预览 |
| **导出 1080×1920 成片** | ❌ | ✅ |
| 订阅消息提醒 | 合成完成 | 合成完成 / 会员到期 / 豆余额不足 |

### 6.5 后台账务能力

- 商家维度：余额（充值豆/赠豆分列）、充值记录、消费明细、AI 调用明细
- 平台维度：**毛利报表**（豆消耗折算 vs DeepSeek 实际账单）、异常消耗告警
- 手动调账全留痕（补偿/扣回，记操作人）

---

## 七、核心接口（REST `/api/v1`）

- `POST /auth/sms/send` · `POST /auth/login`
- `GET|POST|PUT|DELETE /stores` · `/dishes` · `PUT /persona` · `POST /stores/:id/default`
- `POST /media/presign` · `POST /media/callback`
- `POST /ai/copy` · `POST /ai/storyboard`（**带 requestId，先冻后扣**）
- `GET /shot-library` · `POST /admin/shot-library/ai-generate`
- `POST /render` · `GET /render/:id`
- `GET /bean/account` · `GET /bean/packages` · `GET /bean/ledger`
- `GET /member/packages` · `GET /membership/current`
- `POST /order/create`（RECHARGE_BEAN / MEMBERSHIP）· `POST /order/notify`

---

## 八、关键链路

**1. 登录**：手动输入手机号 + 短信验证码 → JWT（access 2h / refresh 30d）。不用 `wx.login` / `getPhoneNumber`，双端一致且避开权限门槛。必须有《用户协议》《隐私政策》勾选。

**2. 素材上传**：预签名 → **直传 COS**（分片 + 断点续传）→ 回执 → ffprobe → 封面。UI 引导"建议 500MB / 1 分钟内"。素材归属 `store_id`。

**3. 粗剪合成**：提交 → BullMQ（并发后台可配，8 核机建议 3–4）→ trim → scale/pad 1080×1920 → concat 硬切 → 四参数整片调色 → H.264/AAC → 回传 COS → 通知。
- **无水印无片尾**，少一步 overlay
- 超时 30min、失败重试 1 次、进度 2s 轮询 + 订阅消息兜底

**4. AI 降级**：DeepSeek 超时/失败 → 兜底模板文案 + 通用分镜，**不扣豆**。

**5. 会员拦截**：非会员点导出 → 弹权益对比 → 引导订阅；订阅成功后自动继续。

**6. 多门店**：注册自动创建默认门店；顶部切换器；AI 生成时把门店名/品类/地区/人设/活动一起进 prompt。

---

## 九、平台差异适配点

| 能力 | 微信 | 抖音（二期） | 适配策略 |
|---|---|---|---|
| 登录 | wx.login | tt.login | 统一手机号 + 短信 |
| 上传 | wx.chooseMedia / UploadTask | tt.chooseMedia / UploadTask | 抽象 `uploadFile()`，按需降级 |
| 支付 | wx.requestPayment | 抖音支付（需审核） | 抽象 `pay()`；不通则隐藏充值入口 |
| 消息 | 订阅消息 | 平台能力待定 | 不支持降级站内消息 |
| 保存 | wx.saveVideoToPhotosAlbum | 对应 API | 失败降级下载链接 |

---

## 十、风险与边界

1. **合同变更**：第零节四项建议打包书面确认（甲方已知悉，按决策执行）。
2. **工期**：30 工作日 + AI豆账务 + 多门店，压力大。抖音端排二期可缓解，建议排期表明确节点。
3. **订阅消息是"一次性"的**：一次授权只能发一条。要在合成完成、会员到期、豆不足多个场景分别引导授权，否则续费提醒发不出去。
4. **服务器**：建议 8 核 16G；FFmpeg worker 与 API 分离进程，并发后台可配。
5. **微信类目**：视频工具类目需提前确认，审核卡住不计入乙方工期（合同 5.2）。
6. **示范视频**：甲方提供（合同不含制作），未到位前显示占位。
7. **AI 成本波动**：DeepSeek 调价直接影响毛利，后台毛利报表是刚需。
8. **支付回调幂等**：微信支付回调可能重复通知，订单状态机必须幂等，否则重复发豆/重复开通会员。

---

## 十一、剩余待确认（v3）

| # | 事项 | 我的建议 |
|---|---|---|
| 1 | 会员是否赠豆 | ✅ 建议（月 1000 / 季 3500 / 年 15000） |
| 2 | 新用户注册赠豆 | ✅ 建议 30 豆 |
| 3 | 非会员预览规格 | 低清 480p 短视频，会员导出 1080×1920 完整 |
| 4 | 门店数量限制 | 非会员 1 家 / 会员不限（或 N 家） |
| 5 | 充值档位 | ¥10 / ¥50 / ¥100，赠豆梯度 0 / +500 / +1500 |
| 6 | DeepSeek 模型型号 | deepseek-chat（V3）还是 deepseek-reasoner（R1，贵且慢） |
| 7 | 服务器最终配置 | 8 核 16G |

**账号与资源（合同 4.2：均以甲方名义注册）**
- [x] 微信支付商户号 —— **申请中**（最高优先级，AI豆与会员全依赖）
- [ ] 微信小程序 AppID + 认证（300 元/年）+ 类目确认
- [ ] 云服务器（建议 8 核 16G）+ 带宽
- [ ] 腾讯云 COS Bucket + 访问域名 + CDN
- [ ] 已备案域名 + HTTPS 证书
- [ ] 短信服务 + 签名与模板报备
- [ ] DeepSeek API Key + 充值（**开通账单导出，用于毛利对账**）
- [ ] 抖音小程序账号 + 企业认证（二期前到位即可）
- [ ] 测试素材（门店信息、菜品图/视频若干）
